===========================================================================
 HP3-SPLITSCREEN - REVERSE-ENGINEERING FINDINGS
 Accumulated while building the split-screen mod (v1 .. v49, 2026-08/09)
 Everything below was verified in the running game unless marked otherwise.
===========================================================================

0. THE GAME
-----------
- Harry Potter and the Prisoner of Azkaban, PC (EA/KnowWonder, 2004).
- Unreal Engine 2. Main modules: hppoa.exe, Engine.dll, Core.dll, D3DDrv.dll,
  HGame.u (680 classes), KWGame.u (302), GamePlay.u (217).
- All split-screen support is STRIPPED from the shipped binaries:
  Engine.SplitScreen, GameInfo.AddPlayer/RemovePlayer, and every joystick-axis
  symbol are absent. The UnrealScript compiler is unreachable at runtime
  (no commandlet dispatch in hppoa.exe; "-make" fails; Editor.dll still
  contains UMakeCommandlet/FScriptCompiler but nothing can reach them).
- Game .u packages are source-stripped: only Engine.u/Core.u/Editor.u keep
  ScriptText. HGame/KWGame/GamePlay expose names, flags and UProperty
  metadata, but no script source.

1. DEAD ENDS (do not retry)
---------------------------
- Recompile UnrealScript with -make: dead (see above).
- Editing game .u packages: source-stripped, and the packaging format's
  export table (v129) is only partially solved (~2000 layout guesses, 0
  valid) - see tooling notes at the end.
- Console route: no split-screen exec exists to call, even with a console.
- Engine.Actor.Mesh is an asset reference, NEVER write it (the mod writes
  DesiredRotation / RotationRate instead - see §7).
- bHidden does not make a pawn invisible on real hardware (works only in
  narrow Wine cases). Hardware honors Location writes, not bHidden.

2. INJECTION
------------
- The mod is a proxy d3d8.dll. Windows (and Wine with
  WINEDLLOVERRIDES="d3d8=n,b") loads the application-directory d3d8.dll
  before the system one. hppoa.exe / D3DDrv.dll import "Direct3DCreate8"
  undecorated - a .def alias entry is REQUIRED or the loader cannot
  resolve the export (symptom: Wine "unimplemented function
  d3d8.dll.Direct3DCreate8").
- One export: Direct3DCreate8. The proxy wraps the real device, hooks
  EndScene/Reset-level flow and the engine's per-view render call, and
  runs all mod logic on the game thread from a PostRender-style hook.

3. HOW THE SPLIT IS RENDERED
----------------------------
- The primitive is UCanvas::DrawPortal (native 480 in Engine.dll):
  ?execDrawPortal@UCanvas@@QAEXAAUFFrame@@QAX@Z   (Engine.dll RVA 0x00BE6A)
  It renders the scene from an arbitrary viewport+actor+eye+rotation+FOV
  into a screen rect. Calling it once per player per frame IS split-screen.
- It is a NATIVE, not reachable through script; call through FFrame/GNatives
  or replicate FPlayerSceneNode directly:
  ??0FPlayerSceneNode@@QAE@PAVUViewport@@PAVFRenderTarget@@PAVAActor@@VFVector@@VFRotator@@M@Z
  ctor args: this=ecx, [esp+4]=Viewport, +8=RenderTarget, +0xC=Actor,
             +0x10=FVector(12B), +0x1C=FRotator(12B), +0x28=float FOV.
- UGameEngine::Draw (Engine.dll RVA 0x0035EE) is the per-viewport entry;
  UInteractionMaster::MasterProcessPostRender (RVA 0x00B5E6) is the hook
  point that already runs per frame with a UCanvas in hand.
- Key derived facts: UViewport::SizeX/SizeY at +0x8C/+0x90;
  UCanvas::ClipX/ClipY at +0x40/+0x44; UCanvas::SizeX/SizeY +0x64/+0x68;
  UPlayer::Actor (the Pawn) at Viewport+0x34.
- Splitting N ways: each strip = full height, width/N. The view count is
  data (Players=N in hp3mod.ini), nothing in the renderer assumes two.
- Measured cost of one extra DrawPortal view was small (tens of percent
  frame time on 2004-era hardware); the split itself never crashed.

4. REFLECTION - THE LOAD-BEARING TRICK
--------------------------------------
- UE2 keeps a live UProperty object for every script field, each carrying
  its own byte Offset. Core.dll exports the global object table
  (GObjObjects) and GetFullName, so ANY field can be found by its path
  string ("Engine.Actor.Location") at runtime - zero hardcoded offsets.
- The only unknown was where inside UProperty the Offset lives. Calibrate
  at runtime against two independently-known fields:
      Actor.Location  = +0x150
      Actor.Rotation  = +0x15C
  The one dword position that satisfies both is +0x4C  ==>  UProperty::Offset
  sits at +0x4C in this build. Every other offset is then a name lookup.
- UFunction parameter lists are recoverable the same way (parameters are
  child objects named Package.Class.Function.Param; read each child's
  class token + the dword at +0x4C). dumpFunctionSig() rebuilds call
  frames byte-accurately with no source. Examples recovered:
      hgame.HPCharacter.castSpell   +0x00 aTarget, +0x04 aTargetOffset,
                                    +0x10 spellClass
      hgame.HPPawn.SpawnSpell       +0x00 spellClass, +0x04 aTarget,
                                    +0x08 ReturnValue
- Verified offsets (this build):
      AActor::Location +0x150   AActor::Rotation +0x15C
      AActor::Velocity +0x168   AActor::Acceleration +0x174
      AActor::Physics  +0x41    AActor::Owner +0x48   AActor::Level +0x9C
      Engine.Pawn.Controller +0x3F8   Engine.Controller.Pawn +0x3F8
      Engine.Pawn.GroundSpeed +0x470
      Engine.PlayerController.Player +0x574  ViewTarget +0x5D8
      hgame.HPCharacter.currentSpell +0x120C (ClassProperty)
- Core.dll symbol notes:
      ?ProcessEvent@UObject@@UAEXPAVUFunction@@PAX1@Z  (RVA 0x02B600)
      GNatives 0x0DB6D8 ; appInit 0x038E60 ; StaticLoadClass 0x04A3E0 ;
      StaticConstructObject 0x046D20 ;
      globals: GIsEditor 0x0A6A4C, GIsScriptable 0x0A6A48, GIsRunning 0x0A6A6C
- Calling engine natives: build an FFrame {vtable@0, Node@4, Object@8,
  Code@0xC, Locals@0x10} or dispatch through ProcessEvent with a zeroed
  oversized parameter block. ALWAYS call on the game thread.

5. PLAYER 2 CONTROL
-------------------
- Movement: per-frame velocity override on the pawn (Acceleration/Velocity
  writes) - collision and gravity behave normally; the pawn walked ~2150
  units across a free-roam level, Z tracking floors down stairwells.
- Jump: call the pawn's own KWGame.KWPawn.DoJump_Player through
  ProcessEvent (so animation + sound stay consistent) - it alone moves
  NOTHING because the jump path bails for AI-controlled pawns. The fix:
  call the script AND write JumpZ into Velocity.Z with Physics=PHYS_Falling.
  The engine then runs a real ballistic arc and lands by itself. Proven
  frozen family: jump pin idle=0 / prep=1 / launch=2.
- Ground physics: for a controlled companion, grounded PHYS 5/6 states
  must be pinned back to 1 (Walking) every frame; physics-dependent
  (PHYS_Projectile-family) holds are terrain-dependent and were dropped.
- Spells: companions have currentSpell (+0x120C) == NULL (the game never
  lets a player be a companion), so PressedFire alone does nothing. Give
  the pawn a spell class, then run its own pipeline:
      ChooseSpell(cls, bForce=1) -> canCast() -> StartCasting(cls,1.0)
      -> castSpell(NULL,0,cls) -> SpawnSpell(cls,NULL)
  Spell classes are ordinary loaded classes resolved by name:
  hgame.RictusempraSpell, DepulsoSpell, SpongifySpell, LumosSpell,
  AlohomoraSpell, GlaciusSpell, LapiforsSpell.
  SpawnSpell returning a named live actor is the ground truth that a cast
  really happened. The spell must be nudged out of the caster along the
  camera direction (camDir*90 + 45Z worked) and given the camera yaw/
  pitch as velocity, or it spawns inside the caster and dies.
- Use/interact: KWGame.KWPawn.CanDoPickupActor() returns true for the
  controlled pawn; then dispatch PickupActor / KWPawn.Trigger.
- AI disown: "soft" release (clear the controller's rotation pull every
  frame) is the default and is reversible; the hard UnPossess path also
  works but is off by default.

6. CAMERA
---------
- Rest camera tuned on hardware: Distance=125, Height=50 ("perfect" -
  frozen), FOV uses the game's unsqueezed mode (FovMode=1).
- Camera collision: slide along walls instead of clipping (line checks +
  pull-in), described in docs/MANUAL.txt.
- Vertical spell aim rides the camera pitch (approx +45/-70 deg range).
- The companion pawn's MESH yaw is re-pulled by the companion AI every
  tick whenever nothing pins it - a standing lock must re-write
  DesiredRotation=(0, viewYaw, 0) + RotationRate=0 EVERY FRAME while the
  player stands still, restoring the saved RotationRate on move/jump.
  One-shot writes lose to the AI within a few ticks.

7. THE CAST PIPELINE AND ITS ANIMATION (the hardest part)
---------------------------------------------------------
Timeline of one cast (measured in logs; hold = keydown->keyup intent):
  keydown -> PressedFire -> StartCasting -> playCastAim (AIM phase;
  glow actor appears at the aim target)  ... fire happens ~1.15s into
  the HOLD (playCast -> castSpell -> Fire -> SpawnSpell -> finalizeSpell
  -> ReleasedFire; keyup timing is irrelevant, ReleasedFire is part of
  the fire sequence) ... the state exits (StateCasting.EndState ->
  GotoDefaultGroundMovementState) ~1.3s after the fire.
- StateCasting telemetry facts:
    * StateCasting.BeginState is logged only ~50% of casts under Wine
      (hardware logs it consistently). HPCharacter.StartCasting fires on
      EVERY cast at aim start - use it as the reliable "a cast began"
      stamp. (Suffix-match care: "HPCharacter.StartCasting" is 24 chars.)
    * StateCasting.Tick stops right after the fire on Wine; on hardware
      it keeps ticking ~1.7s into the wind-down. Tick-recency is NOT a
      portable "cast still running" signal.
    * Wine never emits StateCasting.AnimEnd (hardware does, ~650ms after
      fire).
- The post-cast problems and their fixes, in order of discovery:
    * HOP: the state exit re-enables physics and the pawn free-falls the
      fraction of a unit the cast pose held her above the floor.
      MEASURED on hardware (v47 log, every cast): 874.75 -> 872.3 in
      ~130ms (vz -61, phys 2) -> snapped back to 874.5 by the landing.
      v38 swallowed PlayFalling + PlayLandingAnimation for 1.5s after
      each exit (100% swallow rate, hardware-proven) - that hid the
      ANIMATION but not the dip, and it also removed the engine's own
      landing animation, i.e. the very thing that used to re-settle the
      pose and the facing. That is why a hop that slipped through the
      swallow "fixed" everything, and why the user could describe the
      hop as the cure.
    * STUCK POSES / WRONG MESH YAW after casts: the exit leaves the mesh
      (animation) out of sync with the actor; the fix is ONE
      ChangeAnimation re-pick shortly after the cast settles.
    * v40-v46 tried polling designs (fixed window after the exit, gated
      by cast-activity clocks). They failed reproducibly: rapid re-casts
      re-armed the window/clocks against the WRONG cast, and the re-pick
      interrupted a fresh cast mid-aim (shipped as v45, pulled).
    * v47 went event-driven with six execution-time gates. Correct, but
      too strict: in the v47 hardware log the re-pick FIRED ONCE IN TEN
      casts. One gate is the visible reason - g_ownCastAt ("3s after a
      cast began") was meant for the mod's own cleanup re-cast, but
      beginCast() stamps it for player casts too, so it delayed every
      ordinary cast. The pose and the facing therefore only came right
      on the casts that happened to hop.
    * v48 - pose cleanup confirmed; the dip guard failed on hardware:
        - STUCK POSE: the cleanup kept its per-cast bookkeeping in
          function-local statics inside finishPendingCasts() that were
          only cleared when a cast's 4s watch expired. A cast that
          began before the previous one's window ended inherited
          "chain already fired / already verified" and got NO exit at
          all (v47 hw log, second cast of the first window: no
          [castclean] line, pose parked 3s until a stale tick-retry).
          Now there is ONE CastRun per cast: created when the spell
          leaves (castRunStart), dropped the moment a newer cast owns
          the pawn (g_beginCastAt moves) or the 4s watch ends. Every
          step is bound to that run, so no stale state can survive a
          re-cast. exit -> verify (EndState seen, or StateCasting.Tick
          stopped; else re-issue the exit up to twice, 700ms apart) ->
          settle -> done, all logged as [cast] run#N.
        - HOP (failed attempt): castDipGuard restored the fire-time Z
          and wrote Physics=Walking on small Falling frames near the
          fire position. It did NOT re-establish Base/Floor through the
          engine, and driveePawn immediately froze Walking back to None
          while idle. The new hardware log disproves "no fall, no hop":
          runs #1/#2/#5 have 12/13/15 cancellations, followed by a real
          dip/landing; runs #4/#6/#8/#9 first fall on MOVEMENT, after
          the guard or the whole run has already ended. Many resets
          were Z 874.75 -> 874.75, vz 0 -> 0, yet the next walking tick
          fired Falling + BaseChange again. Extending timers or the
          60-unit radius would only keep the loop going longer.
        - POSE / YAW: the ChangeAnimation re-pick now runs once per
          cast, 450ms after a verified exit, the first moment the pawn
          stands still - and it stays armed for the rest of the run, so
          a walk-away still gets it when she stops. The actor's
          Rotation and DesiredRotation are re-written to the view yaw
          at that same instant, so the animation the engine picks is
          the one facing the camera.
    * v50 (implemented; NOT YET VERIFIED IN HP3):
        - The previous cast path could still look like "animation only" on
          map objects because default AimedCast=0 fired a straight projectile
          with spellTarget=None. Player 1's real controller/cursor supplies a
          gameplay target for SpellTrigger/cast objects. P2+ now does the same
          at release: trace the camera spell line in 3D, pick the castable
          SpellTrigger under the reticle, write HPCharacter.spellTarget, pass
          that target into playCast/castSpell/SpawnSpell, and invoke the
          target's own HandleSpell/OnHandleSpell/SpellHit/Trigger function as
          a safety net. The target search is narrow (SpellTrigger or cast+
          Trigger names only) so missed casts do not fire arbitrary overlap/
          cutscene triggers. Logs are [castgame] target/activating.
        - HARDWARE VERDICT: statues and jump pads still did nothing. Three
          reasons, all structural: (1) statues/pads are not named
          "SpellTrigger", so they never became targets; (2) the spell fired
          was the caster's default (Rictusempra) - a Lapifors statue or a
          Spongify pad ignores it; (3) the "safety net" was Trigger(caster,
          caster) - spell objects react to a SPELL TOUCH, not to Trigger.
    * v51 (implemented; NOT YET VERIFIED IN HP3) - THE ORIGINAL CAST PATH:
        - No HP3 script source exists anywhere public. The path was
          reconstructed from two same-family sources: the HP2 (Chamber of
          Secrets) script decompile (github metallicafan212/HP2UScriptDecompile,
          same hgame lineage: baseSpell/spellTrigger/HPawn.OnSpellHit/
          HandleSpell<Name>/baseWand.CastSpell/SpellCursor) and the Shrek 2
          KWGame export (github Master-64/Shrek2-LevelEditor, the exact
          KnowWonder framework HP3 is built on: KWHeroController.Cursor
          (SelectCursor), KWPawn, KWJumpPad/BouncePad, and the engine-level
          Actor property block var(Reaction) Class<Projectile>
          vulnerableToClass + var(Targeting) SizeModifier/CentreOffset/
          GestureDistance/bMustClickToTarget present on EVERY actor).
        - How player 1 activates a statue/pad (reconstructed):
            1. SelectCursor/SpellCursor traces the camera line and accepts
               only actors whose vulnerable-to value is set (HP2:
               eVulnerableToSpell != SPELL_None; KW: vulnerableToClass !=
               None). aCurrentTarget/aPossibleTarget hold the pick.
            2. LockOn -> wand.ChooseSpell(target.vulnerableTo): THE SPELL IS
               CHOSEN FROM THE TARGET. castSpell(aTarget, aTargetOffset,
               spellClass) gets the target and the hit-point offset.
            3. baseSpell extends Projectile, TargetActor set -> per-tick
               homing (UpdateRotationWithSeeking). No TargetActor = no homing.
            4. Collision: spell.ProcessTouch(Other, HitLocation) ->
               HPawn(Other).HandleSpell<Name>(self, HitLocation) /
               OnSpellHit(self, HitLocation); spellTrigger via Trigger.Touch
               (IsRelevant: baseSpell(Other).SpellType == eVulnerableToSpell,
               so a generic Trigger() call never activates it). SpongifyPad.
               HandleSpellSpongify lives INSIDE a state (stateDisabled).
            5. Autohit: baseWand fires LastCastedSpell.ProcessTouch(aTarget,
               aTarget.Location) directly for close targets. That is the
               call we replicate: the game's own dispatch, from our timing.
        - v51 mechanics (dllmain.cpp, "CAST GAMEPLAY, PLAYER-1 FAITHFUL"):
            * cgBuildIndex: one object-table pass indexes every
              ClassProperty/ByteProperty named *vulnerable*, SizeModifier,
              CentreOffset, and every Function named HandleSpell*/
              OnHandleSpell/OnSpellHit/SpellHit/HitBySpell*/ProcessTouch/
              Touch/Trigger (class level, plus state-level spell handlers).
              It also calibrates two previously unknown offsets the same
              way UProperty::Offset was calibrated: UObject::Class (the
              dword that equals Core.StructProperty in Engine.Actor.Location,
              Core.Function in Engine.Actor.Trigger, ...) and
              UStruct::SuperField (the dword that equals Engine.Actor in
              Engine.Pawn, Engine.Controller in Engine.PlayerController,
              ...). The chain is VERIFIED on a known pawn (must reach
              Engine.Pawn and Engine.Actor) before anything trusts it;
              otherwise the v50 name-only rules remain as fallback.
              Pointers are only dereferenced as objects when they are in
              the known-class set (binary search over every "Class ..."
              object) - the crash guard for property values.
            * cgScanCandidates: actors (chain reaches Engine.Actor, not a
              projectile, not the trio, not cursors/emitters) whose
              vulnerable class value is a live class, or whose vulnerable
              byte is non-zero, or SpellTrigger-family, or that override a
              specific HandleSpell<Name> below the shared bases (HPPawn/
              HPCharacter/KWPawn declare stubs for all of them - those do
              not count). Spell class per object: the vulnerable class
              value, else a "*spell*" ClassProperty on it, else derived
              from the handler name (HandleSpellLapifors -> hgame.
              LapiforsSpell / spellLapifors). baseSpell/Projectile values
              mean "any spell" and are not cast.
            * cgPickTarget: 3D corridor from the real spawn point along the
              camera line, CollisionRadius+90 (x SizeModifier), LOS by
              FastTrace to the aim point (Location+CentreOffset) or a point
              pulled back in front of the object. Same picker drives the
              aim glow (locks onto the object) and the fire.
            * beginCast/fireCast: spell = target's class (ChooseSpell/
              StartCasting/castSpell/SpawnSpell all receive it; the pawn's
              own spell is remembered per pawn and restored for untargeted
              casts); aTargetOffset = CentreOffset; the spawned projectile
              keeps TargetActor and, if the game left it stationary, is
              given velocity toward the aim point.
            * cgArmHit/cgTick/cgDeliverHit: after the fire a pending hit
              watches the projectile (slot + class-pointer liveness, no
              name compares per frame); when it gets within
              CollisionRadius+40 of the aim point or the flight window
              (dist/Speed + 0.7 s, 0.9-3.5 s) closes with it still alive,
              the delivery runs: trigger-family target -> target.Touch(
              spell); spell class overrides ProcessTouch -> spell.
              ProcessTouch(target, spellLocation); else target.
              HandleSpell<Name>(spell, hitLoc) / generic handler / spell.
              Touch(target). Params are filled by NAME+TYPE from the
              function's child properties (ClassProperty -> spell class,
              Object named *spell* -> spell actor, *instigator/caster/
              owner/pawn/source* -> caster, other Object -> the other
              actor, Struct *loc/hit/pos* -> hit location, Float *charge*
              -> 1.0) and each layout is logged once as [castgame-fn]. A
              projectile that died on its own hit something naturally (the
              engine already dispatched) and is left alone.
            * Telemetry for the proof: the ProcessEvent detour now also
              logs NON-pawn events whose name is a spell/touch/trigger/
              bounce function (self + function + first Object parm), and
              cgWatchP1 opens up to three such windows per level when
              player 1's spellTarget changes, logging his target/current
              spell and the cursor's aCurrentTarget/aPossibleTarget with a
              [castgame] description of that object (class, vulnerable
              value, handlers, whether OUR rules list it). A hardware log
              with one P1 cast and one P2 cast on the same statue is the
              acceptance test.
        - Build note: no MinGW in the sandbox; the DLL was cross-built
          with zig (clang) -target x86-windows-gnu from the same single
          TU (exports Direct3DCreate8 via the .def, imports winmm/user32/
          kernel32 + UCRT api-ms-win-crt-*). The Actions workflow still
          builds with MinGW; both are 32-bit PE and the code has no
          compiler-specific parts (no SEH, no inline asm).
    * v49 (implemented; policy tested, NOT YET VERIFIED IN HP3):
        - Preserve the working v48 exit call set, timing and one anim
          re-pick. Remove castHoldFloor/castDipGuard entirely.
        - Capture a ground-handoff candidate immediately BEFORE the
          exit, not at fire (run #3 walked beyond the old fire radius).
          After the chain and fresh input sampling, invoke the native
          execSetPhysics(PHYS_Walking) with a discovered ByteConst token.
          Native functions need an FFrame, not ProcessEvent parameters.
          IntConst is NOT interchangeable with ByteConst: it writes four
          bytes into a native byte parameter's one-byte storage.
        - FindBase, when its C++ export is available, re-probes the
          current collision support even if SetPhysics was a no-op.
          Never restore Location, a saved Base pointer, or a guessed
          Floor vector. Accept only a live Base and a walkable normal.
        - Leave PHYS_Walking on through at least two subsequent drive
          frames and 50ms of verification before idle may freeze it.
          A second fall is a FAILED handoff, not another opportunity to
          pin physics. Missing support releases to Falling; jumps and
          special/airborne movement veto the handoff. The whole attempt
          is bounded to 250ms, not extended until the player walks.
        - No default animation swallowing. NoDip=0 is the legacy
          animation-only diagnostic fallback; it does not restore Z.
        - beginCast now drops the old run and stamps ownership itself:
          ALL the supplied v48 fires logged begin=0 because event
          harvesting was gated by an existing run's interest window,
          and pelogStop actually uninstalls the purportedly permanent
          hook. Recovery must not depend on diagnostic logging being on.
          Runs also bind to the exact pawn and are reset on level change.
        - [castground] logs native Base/Floor before/after and whether
          Walking survived validation. The v48 log did NOT include these
          field values: lost contact is the working explanation, not a
          hardware-verified measurement of the specific field at fault.
    * Frozen standing Z is not evidence of floor support. Both the old
      standing-sample ring (v35-v37) and the fire-time Z (v48) sampled
      the mod's own PHYS_None pose, not a collision query. Restoring that
      number cannot establish a valid walking base. Neither remains in
      the post-cast recovery path.
    * The aim glow (the game's own cursor actor) is the only reliable
      live signal of the AIM phase: StateCasting does not Tick during
      aiming on either platform, so an in-progress aim is invisible to
      state-tick telemetry and is pre-fire on the fire stamp.
- Engineering lessons, generalized:
    * Compare against SNAPSHOTS taken when the need was armed, never
      against live clocks: a "chain stamp" set at every exit is
      re-stamped by later exits and makes stale-vs-new comparisons
      meaningless (this defeated five veto variants).
    * Judge fixes by measured ground truth (the log's raw value dumps),
      not by grep heuristics over the log: the cleanup chain
      legitimately logs a StartCasting line between a cast and its
      re-sync, which looks exactly like a "misfire" to a grep.
    * strncmp suffix matching: get the length right; a 29-vs-24 char
      bug silently disabled a whole detection path for a day of runs.
    * Single-variable discipline for animation changes; prefer reverting
      to proven behavior over adding new gating.
    * NEVER keep per-event state in function-local statics whose reset
      is tied to a different event's timeline. v48's stuck-pose bug is
      the textbook case: "clear the per-cast flags when the watch window
      expires" is not the same event as "a new cast started", and the
      two diverge exactly when the user does the thing users do - cast
      again quickly. Model the lifetime explicitly (one struct, reset
      where the event happens).
    * A guard that fires on 1 of 10 casts is a bug, not a safety margin.
      Count outcomes in the log before calling a fix done: the v47 log
      holds one [realign] FIRE for ten casts, and that single number is
      the entire user report.

8. PHYSICS NOTES
----------------
- PHYS enum in play for pawns: 0 None, 1 Walking, 2 Falling,
  5/6 grounded variants (spawn/level-load states), plus projectile states
  during casts.
- Hardware honors Location writes on pawns; bHidden is not real on
  hardware. Never write Engine.Actor.Mesh (asset data).
- A recurring far-hall crash has phys=5 as a harbinger (the pawn standing
  in a 5/6 grounded state at the wrong spot). Pre-existing in the retail
  game; the mod pins 5/6 -> 1 for controlled pawns which also removes it
  on the paths it controls.

9. WINE VS HARDWARE (portability notes)
---------------------------------------
- The mod ships for Windows; the whole test rig (Wine + Xvfb + a d3d8
  native override + a one-byte Core.dll patch 0x595A3=E9->EB to skip a
  launcher check) exists only to develop it headlessly. Never ship the
  harness: a deliverable is d3d8.dll + hp3mod.ini + README only.
- Behavioral differences that mattered (see §7): BeginState logging ~50%
  on Wine, Tick stops at fire on Wine (~1.7s on hw), AnimEnd never on
  Wine. A veto stack tuned on Wine alone can still be wrong on hardware;
  the v48 design (one run per cast, verified by the state's OWN events
  plus tick silence, with an explicit "no telemetry = trust the chain"
  branch for Wine) is platform-neutral because it depends on none of
  the shaky signals.
- Wine runs die silently late in long sessions every time - environment,
  not the mod; judge by log completeness.

10. TOOLING
-----------
- In-process telemetry through the same ProcessEvent detour that powers
  the mod: named-event capture with per-pawn filtering (pelog), periodic
  position samples (castwatch), actor-vs-view-vs-mesh yaw samples
  (yawwatch). The engine names every step; reading a timeline beats
  guessing. A [verdict] line per jump/cast round-trip makes regressions
  greppable.
- upkg2.py (package dumper): name tables and objects export fine after
  stripping the first byte of package names; the v129 export table layout
  is still unsolved (do not brute-force it).
- Build rig: i686-w64-mingw32-g++ -O2 -shared, single TU, .def alias,
  link winmm. See build.sh.

11. OPEN ITEMS / BACKLOG
------------------------
- Far-hall crash (pre-existing; phys=5 harbinger) - root cause unknown.
- The "D-hunt" (an unidentified object/field named D in early dumps).
- Per-player HUD (the vanilla HUD is single-player aware only).
- The tiny idle-restart stutter on CLEAN casts (the re-sync re-picks the
  idle animation after every cast even when the exit settled perfectly).
  v48 re-picks on every cast by design - pose and yaw correctness
  outranks this cosmetic. The honest gate, if it ever annoys, is a real
  mesh-yaw measurement: Engine.Actor.Mesh is an ASSET reference, so the
  current meshYawDelta() reads garbage (it reports mesh=0 in every hw
  log - see the v41 note in §7). Finding the real mesh component
  offset would let the re-pick fire only when the pose is actually
  wrong.
- upkg2.py v129 export table.
12. V52/V53 NATIVE AIM ICON NOTES (2026-09-08)
----------------------------------------------
- v52 hardware (Save0, split ON): P2 seek particles native and correct, but
  the locked SpellGesture never spawned - every real target logged
  "no readable default.SpellIcon", so the legacy Sparkle_1/3/7 additive
  marker drew over the native effect for the whole hold. After a successful
  cast (hover state changed) it glowed like P1; a fresh hold reverted.
- Root cause: the icon read assumed hgame.baseSpell.SpellIcon is a plain
  "Texture" at a fixed offset. Sibling-engine evidence (HP2 UScript
  decompile, metallicafan212/HP2UScriptDecompile): spell subclasses may
  shadow the base declaration, and the gesture glyphs are wet/shader
  materials (SpellCursor.GetGestureTexture feeds WetTexture
  'SpellShapes.SpellFX.<Spell>Wet1' to the same GestureSprite.ChangeGesture
  that our lock path calls). A strict "Texture " check rejects exactly the
  values the game passes.
- v53 fix (src/native_aim.h): resolve <spellClass path>.SpellIcon on the
  class's own chain via findObjectByPath + calibrated UProperty::Offset
  (cached, cleared with the bindings), accept any material-family object,
  pass it to ChangeGesture untouched, and on failure log the exact gate
  (class, +0xX slot, None/unreadable/wrong-family value) instead of a bare
  miss. Anchor fallback remains hgame.baseSpell.SpellIcon.
===========================================================================

13. V54 COOPERATIVE SUSTAINED-CAST BRIDGE (2026-09-08; IN-GAME VERIFICATION PENDING)
-------------------------------------------------------------------------------
- The requested behavior is general: a Player-2 or Player-3 continuous cast on
  an existing object which genuinely requires Harry, Hermione and Ron to hold
  together. It is not a Pixie-well-specific completion or a one-shot impact.
- No HP3 package source or installed HGame.u was available to establish a
  class/function name for that interaction. In particular, an earlier
  CompanionSpellTrigger name was only a hypothesis and is NOT used as a
  hard-coded admission rule.
- v54 therefore behavior-certifies target classes live. While the original
  game is running (including split OFF), cgWatchP1 observes P1's real
  SpellCursor.aCurrentTarget. A class is recorded only after P1 remains locked
  on that exact actor for 350 ms AND both real companion pawns' spellTarget
  fields independently equal it. This is logged as [coopcast] CERTIFIED and
  lasts only until level-cache reset. Exact/subclass matching thereafter is
  pointer/class-chain based, never object-name based. A throttled current-map
  cache also finds only those certified actors when a legitimate cooperative
  actor has no vulnerableToClass metadata and is absent from g_cgCand; the
  same reticle corridor, object slot/class identity, and FastTrace checks apply.
- Once certified, a P2/P3 held-cast key is a pure state machine keyed by player
  slot + object pointer + UClass pointer + object-table slot. It requires an
  uninterrupted interval exceeding 10,000 ms. Release, aim loss, target replacement,
  slot reuse/class change, split shutdown and level cache reset clear it. The
  host regression test covers continuity, reset, class identity and unsigned
  GetTickCount wraparound.
- At maturity, the bridge first uses the original P1 SpellCursor.LockOn only
  when reflection sees precisely one target-named ObjectProperty and no other
  object input (the known family shape is LockOn(Actor TargetActor)), its paired
  UnLock/Unlock has a fully reflected no-input layout, and the live P1 cursor
  confirms it retained the requested actor. It then
  sets the verified target spell/target fields and starts the known
  PressedFire -> StartCasting(class, 1.0) -> playCastAim hold path on the real
  non-holder Harry/Hermione/Ron actors. The holder retains their actual P2/P3
  hold. If the cursor/function/ABI bridge cannot be established, v54 restores
  its empty provisional record and leaves the trio unchanged. This is
  deliberately a state bridge, not a call to target Trigger,
  Touch, ProcessTouch, a generic spell handler, repeated projectile impacts,
  or an unknown completion boolean.
- On cancellation, borrowed actors receive StopCasting before ReleasedFire so
  they do not spawn duplicate projectiles; P1's cursor gets UnLock; fields are
  restored only if they still hold the value borrowed by the bridge. A real
  controller's intervening different target is never overwritten. If another
  human-controlled hero is already casting at a different target, or starts a
  normal cast while the bridge is active, the bridge restores immediately and
  blocks the holder's still-held attempt until release/target loss/new hold.
- Native hardware/VM behavior remains unverified. The implementation logs each
  proof, hold start, reflected cursor call, borrowed hero state, activation and
  restore reason so a real HP3 log can confirm or reject the inferred route.

14. V55: V54 CO-OP REGRESSION - SPLIT-ON CERTIFICATION DEAD GATE (2026-09-09)
-----------------------------------------------------------------------------
- Hardware capture showed a >10-second P2 hold on Save0.CompanionSpellTrigger10
  never entering the shared state: it kept producing ordinary single casts and
  no [coopcast] CERTIFIED line ever appeared, even though the same session's
  log contained "[castgame] PLAYER 1 cursor aCurrentTarget ->
  Save0.CompanionSpellTrigger10" (Player 1 demonstrably locked the object).
- Root cause 1 (certification never ran while split was ON): coopObserveP1()
  was called only from !g_splitOn branches in cgWatchP1, and inside the
  function the all-three test required both companions' PAWN spellTarget to
  equal P1's cursor target. During a live split session the companions are
  AI/mod-driven and their pawn spellTarget never demonstrates a shared hold
  (the mod's native-aim adapter only writes spellTarget on cast release), so
  the whole certification path was structurally dead in the only mode where a
  P2/P3 >10s hold can happen. v55 calls coopObserveP1() every frame in both
  modes and, while split is ON, treats P1's sustained genuine stock cursor
  lock (>=1200ms continuous) as the live class evidence. Split OFF keeps the
  strict all-three demonstration (>=350ms) unchanged.
- Root cause 2 (scan suppression, separate report): handler-only admission in
  cgScanCandidates was gated on g_cgNVulnDecl == 0 for the whole level, so a
  single unrelated vulnerable-typed class suppressed CGC_FN classification for
  every otherwise-unnamed actor in the map. Admission is now per object: a
  class with 1-2 own HandleSpell<Name> handlers below the shared bases is
  admitted regardless of what other classes declare.
- Certification remains behaviour-only (no guessed class names); the >10s
  continuous-hold threshold, the real P1 SpellCursor.LockOn bridge and the
  real trio StartCasting path are unchanged; no Trigger/Touch/impact/completion
  shortcut was added. Hardware re-verification pending (v55 build).

15. V56: V55 P1-HOLD STILL DEAD - 10S P1 CURSOR LOCK DIRECTLY FIRES THE HOLD
    + 250MS FLICKER TOLERANCE (2026-09-09)
-----------------------------------------------------------------------------
- Hardware capture of v55 confirmed the v55 cert fix worked in principle
  (a deliberate 1.2s+ continuous P1 cursor lock emits [coopcast] CERTIFIED),
  but a >10-second P1 hold on Save0.CompanionSpellTrigger10 still never
  produced a triple cast. The stock P1 cursor was flicking its aCurrentTarget
  through None for a frame or two between controller updates (cursor log:
  ...->CompanionSpellTrigger10 -> None -> ...->CompanionSpellTrigger10
  interleaved every 1-2 frames). v55's coopObserveP1 cleared the cert
  pending-since on every None frame, so the 1200ms split-ON minDwell was
  almost never reached, and the cooperative path was never armed before
  P1's normal cast released.
- Separate issue: v55's design only made P1 a *certification* source, not
  a holder. coopStart / coopTick / coopMonitor all rejected holder < 1, so
  even a P1 cursor lock that did certify a class still required a P2/P3
  >10s hold to fire the triple cast - P1's own >10s hold was a no-op
  past the cert step.
- v56 fix 1 (P1 holder): coopStart / coopTick / coopMonitor /
  coopStopHolderForSplitShutdown now accept holder=0 (P1). coopStart's
  borrowed-hero loop is unchanged - it still skips p == holder, so for
  holder=0 Hermione and Ron receive the StartCasting/PressedFire sequence
  while the engine drives P1's own cast state. coopRestore's "stop before
  release" loop calls StopCasting/ReleasedFire only on heroes whose
  hero->started is true; for holder=0 that excludes hero[0] (P1's started
  is FALSE because the mod never borrowed P1's cast state - the engine
  already had it), so the engine's normal P1 cast release fires P1's
  spell unchanged and the borrowed heroes are cleaned up without
  spawning duplicate projectiles.
- v56 fix 2 (P1's own 10s cursor lock fires the hold): a new
  coopP1HoldTry() runs every frame in cgWatchP1, after coopObserveP1.
  When g_p1CursorLockedAt is set and the cursor has stayed on the same
  object for >= g_coopCastHoldMs (default 10s, set by [actions]
  CoopCastHoldMs=10000), coopP1HoldTry certifies the class (if not
  already in g_coopProof[]) and calls coopStart(0, candidate, key). The
  candidate is the cursor's exact aCurrentTarget object, found via
  cgFindCand or the certified-only g_coopDiscovered table; a static
  ad-hoc CgCand is built if neither list contains it. The single
  uninterrupted 10s lock is BOTH the cert evidence and the fire
  condition, so a deliberate P1 hold on a CompanionSpellTrigger class
  enters the shared hold without depending on a separate P2/P3 >10s
  attempt.
- v56 fix 3 (flicker tolerance, 250ms grace): a new g_p1CursorLockedTarget
  / g_p1CursorLockedAt / g_p1CursorLastSeen / g_p1CursorFired quartet
  tracks P1's cursor pointer and tolerates a brief None or different-
  target gap of up to kP1CursorGrace (250ms). A gap longer than 250ms
  resets the dwell and the latch; a different object pointer also
  resets the dwell. The cert's pendingSince is anchored to
  g_p1CursorLockedAt (the first-seen-at) so a frame-level None does
  not restart the 1.2s cert dwell either. The same grace applies to
  g_p1CursorFired: a real release (>250ms gap) re-arms the 10s fire
  path for a subsequent lock, while a frame-level None inside the
  grace window does not.
- v56 unchanged from v55: the v55 cert mode split (split-ON P1 lock
  admits the class; split-OFF keeps the strict all-three demonstration),
  the per-object handler admission in cgScanCandidates, the stock P1
  SpellCursor.LockOn/UnLock bridge, the real trio StartCasting path, the
  native SpellGesture icon-chain / wet-shader fix and 770-unit aim. No
  Trigger / Touch / impact / completion shortcut. The P2/P3 >10s hold
  path is unchanged; v56's relax-of-holder-bound is a superset change
  that does not modify the holder=1 / holder=2 flow.
- Build environment: i686-w64-mingw32-g++ is not available on this build
  host. The v56 source changes have been syntax-checked by running the
  host policy tests (cast_ground, aim_policy, coop_cast, native_aim)
  green; the full d3d8.dll cross-compile is deferred to a host with
  mingw installed (the existing build.sh is unchanged). bin/d3d8.dll on
  disk is still the stale v54 build; the v56 source banner is "build
  v56" and must match the rebuilt binary.
- Hardware re-verification pending (v56 build): the user's v55 test
  trace showed the cursor flicker; v56 should now (a) certify the class
  despite the flicker (cert dwell anchored to first-seen-at) and
  (b) fire the cooperative path on P1's own 10s cursor lock without
  requiring a separate P2/P3 >10s attempt. P2/P3 >10s hold on a
  certified class is unchanged.

16. V57: HOW THE GAME HOLDS THE HOVERING TRIO CAST + ANY-CHARACTER 10S HOLD
    + LEVEL-TRAVEL GPF FIX (2026-09-09)
-------------------------------------------------------------------------------
- Hardware verdict on v56 (the user's log): still no triple cast. The v56
  session held a cast on Save0.CompanionSpellTrigger10 for ~28 SECONDS as the
  split-screen player (Hermione, pad) and no [coopcast] line ever appeared.
  Root cause: the >10s hold state machine never accumulated a single
  millisecond. coopTick(1, hermione, held) called coopPickTarget every frame;
  cgPickTarget DID find the trigger as a normal [vulnerableTo] candidate (the
  fire-time log proves it: "p1 target -> CompanionSpellTrigger10 ray=626
  perp=16"), but coopCandidateTarget rejected it because the class was not in
  g_coopProof - and certification is structurally unreachable on hardware:
    * split OFF requires BOTH companions' PAWN spellTarget to equal P1's
      cursor target for 350ms. The v56 pelog window 2 proves the stock
      companion join does not satisfy this: while Harry held the trigger and
      Hermione demonstrably entered hgame.HPCharacter.StateCasting.BeginState
      (+1981ms) and Tick, no CERTIFIED line ever appeared - the native recruit
      does not express itself as both pawn spellTarget fields simultaneously
      (and Ron never joined inside the captured window at all), so the
      all-three field test can never pass.
    * split ON requires P1's OWN cursor lock for 1.2s - but during a split
      session the holder is Player 2 or 3; P1's cursor (Harry's) is unlocked
      the whole time. The 28-second hold was therefore rejected every frame
      ("a normal visible lock belongs to an ordinary object" -> return NULL),
  resetting the dwell to zero. The v56 P1-cursor-lock path never ran either,
  because the pre-split Harry hold lasted only ~2s before the user switched
  to split-screen.

- HOW THE GAME ACTUALLY HOLDS THE HOVERING TRIO CAST (user question, answered
  from the HP2 family source + the HP3 hardware pelog):
    * HP2 decompile (HGame/Classes/SpellCursor.uc, same KnowWonder KWGame
      family): SpellCursor.LockOn(Actor TargetActor) is the engine's own
      "hovering this object" transaction. It runs
      baseWand(PlayerHarry.Weapon).ChooseSpell(TargetActor.eVulnerableToSpell)
      (the spell comes FROM the target), sets aCurrentTarget = TargetActor,
      turns on the gesture wet-texture FX sized from the target's collision
      dims, and starts the locked-on sound loop. UnLock() clears
      aCurrentTarget/aPossibleTarget and hides the gesture. The cursor lock,
      not a projectile, is the hold's anchor.
    * HP3 hardware evidence (v56 pelog window 2, split OFF): the engine
      player's held cast + that cursor lock is what recruits the companions.
      ~700ms after Harry's StateCasting began with the cursor locked on
      CompanionSpellTrigger10, Hermione's controller (HPCompanionController)
      put her pawn into StateCasting on its own - no player action, no
      pawn-spellTarget field write visible to the v54 test. The trio hold is
      therefore: one character's hovering lock + the companions' controllers
      joining + the trigger's own script deciding when the converged holds
      complete it. Ron did not join inside the window (distance/state
      dependent), which is why the stock game sometimes needs the player to
      keep holding - exactly the case the user wants the mod to guarantee.
    * The mod's equivalent of "the same effect" for ANY character is what
      coopStart already drives: write the verified target/spell fields on the
      real trio, then the engine's own StartCasting(class, 1.0) /
      PressedFire / playCastAim state entry on the non-holders, plus the real
      P1 SpellCursor.LockOn when its reflected ABI is safe. No Trigger/Touch/
      impact/completion shortcut.

- v57 hold admission (the user's directive: "apply the same effect if target
  is getting held in cast for more than 10 seconds by any character"):
    * coopCandidateTarget no longer requires a proof-table class. Any living
      CgCand qualifies - candidates are genuine cast targets by construction
      (live vulnerableToClass metadata, a spell handler, or a stock
      SpellTrigger-family actor: exactly what the engine's own cursor
      accepts). The deliberate >10s uninterrupted hold by any ONE character
      (P1 cursor lock, or a P2/P3 held cast under the same reticle rules) is
      now the only intent gate. g_coopProof is still collected and still
      feeds coopRefreshCertifiedCandidates (discovery of cooperative actors
      with no targeting metadata) - it just cannot block the hold anymore.
    * coopP1HoldTry no longer aborts when the proof table is full.
    * P1's active hold is maintained/restored keyed by the ENGINE's cursor
      lock (g_p1CursorLockedTarget), not by a camera-ray pick that can
      legitimately disagree with the stock cursor; coopTick(0, ...) is now
      maintain/restore only, coopP1HoldTry is the single P1 arming path.
    * Heroes the game itself already recruited on the exact target
      (savedTarget == target and the spell matches) are marked alreadyHolding:
      not restarted with StartCasting, not force-stopped on restore - the
      engine owns their hold. Everything else is borrowed as before.
    * The stock P1 SpellCursor.LockOn bridge is now best-effort: if the
      cursor's LockOn/UnLock cannot be reflected safely (the strict ABI
      checks are unchanged - an unsafe call is still never made), the trio
      borrow proceeds without it and the log says so, instead of cancelling
      the whole hold as v54-v56 did.
    * The P2/P3 hold gets the same 250ms target-flicker grace the P1 cursor
      lock got in v56 (hp3coop::update now takes graceMs; the host regression
      test covers grace continuity, boundary, post-fire reset, wrap and the
      graceMs=0 legacy behaviour).

- V57 CRASH FIX - hard GPF entering Hogwarts as Harry, split off:
    UObject::GetPathName <- UObject::GetPathName <- UObject::GetFullName
    <- FPlayerSceneNode::Render <- UGameEngine::Draw <- UWindowsViewport::Repaint
  Diagnosis: UObject::GetFullName is exactly what the mod's objName() calls,
  and the crash dialog's stack walker skips the mod's detour frames (MinGW
  -O2 omits frame pointers), attributing the call to FPlayerSceneNode::Render
  - the engine function that invokes UInteractionMaster::MasterProcessPostRender,
  which the mod hooks. objName was being handed CACHED actor pointers
  (g_pawn[0] in cgWatchP1, g_origCursor, g_camActor, cursor/field targets)
  whose level had just been destroyed. IsBadReadPtr passes on freed-but-
  still-mapped UObject memory, and GetPathName then walks the freed Outer
  chain into memory the new level's allocations had started to reuse.
  The level-change invalidation was only sampled every 45th PostRender, so
  every cached pointer stayed live-looking for over half a second (at 80fps)
  right through the travel. Fixes:
    * New cgLiveObject(): a pointer is only named/dereferenced if it is a
      member of the live GObjObjects table (the engine's own liveness record;
      everything runs on the game thread). Applied at every cached-pointer
      site: getPawn, cgWatchP1 (pawn, spellTarget, currentSpell, cursor,
      aCurrentTarget, aPossibleTarget), findOrigCursor, findP1Cursor,
      coopActorAlive, coopP1HoldTry's locked target, the busy-target name in
      coopStart, destroyAimFX (Destroy on a freed emitter would also crash),
      setAimFXPaneVisible (parking writes), and the split pawn-FX teardown.
      actorInCurrentLevel additionally refuses a stale g_camActor (memoised
      250ms per camera pointer - it sits on the candidate-scan hot path).
    * The level-change detector now runs EVERY PostRender frame (before any
      early return) and drops g_camActor instead of naming it when it is no
      longer live; the caches are therefore invalidated within one frame of
      travel instead of 45.

- Build note: this host cannot install mingw-w64 (sandbox blocks the apt
  mirrors). v57 was cross-compiled and link-verified locally with
  zig cc 0.16 (clang + the bundled mingw-w64 i686 sysroot, x86-windows-gnu)
  producing the same Direct3DCreate8/Direct3DCreate8@4 export pair; the
  canonical build.sh (i686-w64-mingw32-g++) and the MSYS2 GitHub Actions
  workflow are unchanged. bin/d3d8.dll is refreshed from this build so the
  checked-in binary finally matches the source banner (it had been stale
  since v54). Host policy tests (cast_ground, aim_policy, coop_cast with the
  new grace assertions, native_aim) all pass.

17. v58 HARDWARE FEEDBACK ON v57 + TRIO-ON-RELEASE + RON AI FIXES

  The user's v57 test report (level-crash fix CONFIRMED working; do not
  regress the cgLiveObject/every-frame detector sites listed in §16):

  A. Trio auto-launches. "After 10 s of holding cast, Player 1 (Harry)
     automatically launches the spell at the object, and after a couple of
     times it breaks: Harry just shouts the spell, plays the animation,
     but doesn't shoot." Intended behaviour (explicit user spec): any
     player holds the cast for 10 s on the target -> the trio forms; when
     the player RELEASES, the trio fires once and counts as the
     cooperative spell. No auto-launch.
  B. Ron's AI broken: "he tries to run back and forth to cast spell
     together with player 1".

  ROOT CAUSES (all confirmed in source; log evidence in the v56/v57 pelog
  windows decoded by pawn-slot: p0=Harry, p1=Hermione, p2=Ron):

  1. AUTO-FIRE = PressedFire on borrowed heroes. coopBeginBorrowedHero
     called KWPawn.PressedFire ("g_fnFire [coopcast hold]") before
     StartCasting(cls,1.0)+playCastAim. On the ENGINE-controlled pawn
     (Harry) PressedFire enters the genuine fire pipeline: StateCast.Tick
     ~+1215 ms, finalizeSpell ~+1465 ms, SpawnSpell ~250 ms later with NO
     release input - the interval between StateCast entry and finalize is
     the wind-up animation length, and the throw fires by itself. On AI
     pawns PressedFire only nudges them into StateCasting. The mod's own
     P2 hold path provably enters AND holds StateCasting with
     ChooseSpell/ShowWeapon/SwitchToFightStanceAnims/canCast/
     StartCasting(cls,1.0)/playCastAim alone - PressedFire was harmful AND
     redundant. v58: removed from the borrow sequence entirely.

  2. "SHOUTS BUT DOESN'T SHOOT" = ReleasedFire-after-StopCasting plus
     restore-before-finalize. coopRestore ran StopCasting THEN
     HPCharacter.ReleasedFire THEN KWPawn.ReleasedFire for BOTH aborts and
     the normal holder release. A ReleasedFire issued after StopCasting
     runs outside StateCasting: the pawn plays the release shout/animation
     and spawns nothing (the exact p2 sequence in the InsideHub pelog
     window 3/3). Worse, on a holder release (p1 SpawnSpell ->
     DepulsoSpell29 in the SAME frame, proven by ordering) the restore
     also returned every hero's currentSpell/spellTarget to the pre-arm
     NULL immediately - the engine's StateCasting finalize re-reads
     currentSpell at AnimEnd ~300-500 ms after the release trigger, so a
     RESTORE-then-fire cycle desyncs the engine pawn: his next cast
     spawns nothing. v58 split the restore into two endings:
       * RELEASE (holder released cast): the holder's own natural path
         fires (engine for P1, mod cast-fire for P2/P3 - the ordering in
         the pelog proves the holder's SpawnSpell precedes the restore in
         the same frame). Each mod-borrowed hero gets exactly ONE
         HPCharacter.ReleasedFire while actually inside the held cast
         state (the stock companion release): three near-same-frame real
         projectiles converge on the shared target, which its script
         counts as the cooperative spell. No StopCasting, NO field restore
         on the fired heroes (finalize needs currentSpell ~300 ms later).
       * CANCEL (target changed/deleted, another player cast, level travel,
         split shutdown): StopCasting only. The two ReleasedFire calls are
         gone - on an idle AI pawn those noise events are exactly the
         cast-without-projectile pattern and fed the Ron oscillation.

  3. RON "RUNS BACK AND FORTH" = v57's any-candidate arming.
     coopCandidateTarget v57 admitted ANY living CgCand (pumpkins,
     spawners, any vulnerableToClass object), so EVERY casual 10 s hold
     force-borrowed Ron/Hermione out of their HPAIController follow state
     into a held cast they must NOT leave, (a) producing the run/cast/run
     oscillation silhouette and (b) issuing the ReleasedFire-until-cancel
     pattern from (2). Additionally coopObserveP1's split-ON branch
     certified any class from a 1.2 s P1 cursor lock, self-poisoning the
     proof table so the hold stayed broadly open. v58 re-gates admission
     to the genuine cooperative class FAMILY (coopClassIsCooperative):
     the CompanionSpellTrigger class token on the class chain (observed
     trio objects on hardware: CompanionSpellTrigger0/2/10) OR a class
     certified this session by the strict all-three companion observation.
     This is a class-family gate, NOT the v54-v56 behaviour-proven gate:
     that one was structurally unreachable on hardware (the game's own
     companion join enters StateCasting WITHOUT setting both companions'
     pawn spellTarget fields, and Harry's P1 cursor is never watched when
     the holder is P2/P3). coopRefreshCertifiedCandidates now discovers
     family actors for metadata-less cooperative objects; split-ON token
     certification requires the family token. A 10 s hold on a
     pumpkin/spawner now stays a plain single-character cast: Ron is no
     longer hijacked and his AI resumes cleanly after any real trio.

  4. While borrowed, AI heroes are pinned: coopMaintain zeroes the XY
     velocity and acceleration of each mod-borrowed, AI-driven hero every
     frame (their controller keeps pushing movement per tick while State
     has them holding a cast pose). Human-controlled heroes are not
     pinned. alreadyHolding heroes (the game's own join) are never pinned.

  5. POST-ARM FLICKER TEARDOWN (new bug found in source review):
     coopTick's ACTIVE-hold branch restored on raw per-frame
     !valid||!sameTarget, but hp3coop::update's kCoopFlickerGrace only
     covers the PRE-fire dwell (grace requires !hold.fired and coopStart
     latches the Ready result). One bad camera-ray frame while still
     holding tore the armed trio down ("holder changed target", cancel).
     v58: CoopCastActive.lastSeenAt anchors its own 250 ms grace for the
     active hold (both P1-cursor and P2/P3-ray holders).

  NOT regenerated behaviour: the holder's own release ordering - the
  mod's cast-fire/fireCast for P2/P3 runs BEFORE coopTick's restore in the
  same frame (pelog), so the coop-preserve-target branch in fireCast still
  sees the live shared target, and the trio's two borrowed shots follow in
  the same tick. The "target gone before the fire completed" cgArmHit
  noise in v57 was the holder's pre-arm NULL field restore arriving before
  the pending-hit arm read it; fire-mode restore no longer writes fields.

  Build note: same host constraint as the v57 build note at the end of §16
  (no mingw-w64; additionally ziglang.org and the GitHub release-asset
  hosts are unreachable from this sandbox), so v58 was cross-compiled
  locally with zig cc 0.16 installed via the PyPI 'ziglang' wheel
  (pypi.org IS reachable):
    zig c++ -target x86-windows-gnu -O2 -s -shared -static \
      -o dist/d3d8.dll src/dllmain.cpp src/d3d8.def -lwinmm -Wall -Wextra
  Direct3DCreate8 export pair verified; bin/d3d8.dll refreshed so the
  checked-in binary matches the v58 banner; host policy tests
  (cast_ground, aim_policy, coop_cast, native_aim) all pass.

  HARDWARE CHECK LIST for the user:
    * 10 s hold on a CompanionSpellTrigger by any player - the trio forms
      (all three wizards in the held-cast pose) and NOTHING fires at 10 s.
    * Release - ONE converging volley (holder + both borrowed heroes),
      once, and the trigger counts the cooperative spell.
    * Repeat several times in a row - no "shout with no shot" cases.
    * 10 s hold on a pumpkin/other object - NO trio, Ron keeps following,
      ordinary single cast on release.
    * Long session exploration - Ron/Hermione AI follow behaviour normal.
    * Level travel in and out of hub areas - no crash (regression check).

17. v60 hardware-report diagnosis and source correction (2026-09-09)
  The supplied startup banner proves the failing run used v59. Its
  same-caster design is retained: a mature 10-second hold arms only, then the
  initiating player's release is one normal shot plus two bonus SpawnSpell
  calls on that same pawn. No companion is borrowed to manufacture x3.

  Three separate v59 delivery defects explain the red light and weak P2 cast:
    * a raw reflected SpawnSpell(cls,target) returned a spell actor but did not
      perform the normal caller's launch setup; the bonuses remained at the
      pawn with zero velocity;
    * both bonuses reused one g_cgPend[player] slot, so shot 3/3 replaced shot
      2/3, and cgTick intentionally began at player slot 1, so neither P1
      bonus watcher ever ran;
    * fireCast retained the charged TargetActor but set gameCand=NULL, dropping
      P2/P3's ordinary-shot delivery watch even though the target still lived.

  v60 completes each raw bonus before the next engine tick: it detaches the
  actor from the caster, starts at chest/wand height clear of the collision
  body, computes a normalized target trajectory, writes Location, Velocity,
  Rotation/DesiredRotation, Speed/MaxSpeed and PHYS_Projectile, and gives each
  bonus an independent pending-delivery slot. Clearance is capped at half the
  target distance so a close target is not overshot. P2/P3's ordinary release
  reuses a stable candidate captured when the charge armed. A bonus that can
  neither launch nor be tracked is destroyed rather than becoming a permanent
  glow. Host trajectory tests cover normal, close, vertical, zero-distance and
  invalid-speed plans.

  v59 correctly removed mod-driven companion borrowing, but P1's unmodified
  SpellCursor still asks the stock companion controller to join a cooperative
  target roughly 0.7 seconds after lock-on. Thus waiting until the ten-second
  arm (or merely removing the old borrow code) cannot prevent Ron's observed
  StateCasting run/cast/run loop. v60 guards movement from the first P1 cursor
  lock on a cooperative-family target: AI-controlled Hermione/Ron slots have
  only horizontal Velocity/Acceleration held at zero. Human split slots are
  skipped, vertical physics is preserved, and no controller link, follow
  field, target/current-spell field, cast function, or release event changes.
  The stock cast lifecycle remains engine-owned and ordinary AI movement
  resumes when P1 unlocks. This source-level guard still requires hardware
  confirmation.

  Local validation: all host policy suites pass; a stripped 32-bit Windows DLL
  cross-build succeeds; Direct3DCreate8 and Direct3DCreate8@4 exports plus the
  v60 startup banner are present. In-game HP3 behavior is not available in the
  sandbox and must be checked on hardware.

18. v61 hardware-report diagnosis and source correction (2026-09-09)
  v60 hardware report (same tester, split OFF + 2-player split): Ron's AI is
  better - joint casts with P1 now happen - but he still runs/casts/runs, and
  the >10-second triple-cast extras remain visual-only with no added effect.
  Two log excerpts pin the causes:

    * P1 solo (split OFF), Save0.CompanionSpellTrigger2: one normal shot plus
      two bonuses launched at 700 u/s over dist 485 with a 1393 ms window, yet
      neither bonus ever logs Touch/ProcessTouch. Root cause: renderSplit-
      Portals' split-OFF branch returned after cgWatchP1 without calling
      cgTick, so P1's bonus pendings armed but never ticked - natural collision
      alone was expected to deliver, and it did not.
    * P2 (split ON), HP3_InsideHub.CompanionSpellTrigger9: all three Depulso-
      Spell actors log Touch+ProcessTouch (delivery worked) yet the trigger
      still does not complete. Same-caster x3 hits are therefore not what the
      trigger counts: it wants distinct casters (the stock game recruits
      Hermione/Ron to hold with Harry for a reason), and three same-tick hits
      from one pawn do not satisfy it.

  v61 keeps "no borrowing" but stops pretending one caster is three. Every
  10-second charged release now fires a VIRTUAL TRIO: the holder's untouched
  normal shot #1, plus two bonus projectiles launched from the other two trio
  members' wand positions at staggered speeds (shot #2 at 100%, shot #3 at 75%)
  with their Instigator fields labelled to those companions (resolved by name
  via Engine.Projectile/Actor.Instigator; Owner stays the holder so bonuses
  still cannot collide with the spawning pawn). The trigger observes three
  distinct casters arriving as three separate hits; no companion controller,
  cast state, target/spell field, or AI is written - only the two new spell
  actors. Dead/missing companions fall back to holder origin/Instigator.
  cgTick now runs on the split-OFF path too, so P1's solo bonuses deliver.

  The guard is rebuilt around the engine tick order. A PostRender velocity/
  accel zero is overwritten by the next AI tick before physics integrates, so
  v60's impulse-only guard could not hold Ron - and pinning from the first
  lock also blocked the legitimate approach-to-range walk. v61 pins only
  companions that are actually casting (the engine's own IsAimingOrCasting
  query, falling back to a held currentSpell), and pins GroundSpeed/AccelRate
  with save/restore - the same mechanism driveePawn uses for idle split pawns
  - while non-casting companions keep walking. Pins release per-slot when a
  companion stops casting, when its slot goes human, on P1 unlock/reset, and
  the flags drop on level travel (fresh pawns spawn with engine stats).

  Preserved: nothing auto-fires at 10 seconds (x3 fires once on release), the
  cooperative-family gate (CompanionSpellTrigger token or behaviour proof),
  native wet SpellGesture icon, 770-unit aim, cast gameplay, and v58 level-
  travel safety.

  Local validation: all five host policy suites pass (cast_ground, aim_policy,
  coop_cast, charged_cast, native_aim); the exact v61 function bodies
  (coopUnguardStockCompanion(s), coopQuietStockCompanionMotion,
  coopInstigatorOffset, coopLaunchBonusProjectile, coopRestore) compile clean
  under g++ -Wall -Wextra against stubs plus the real charged_cast.h/
  coop_cast.h, and a brace/paren audit of dllmain.cpp balances. No Windows
  cross toolchain exists in this sandbox (mingw/zig unavailable, release-asset
  host unreachable), so dist/d3d8.dll and bin/d3d8.dll could NOT be rebuilt
  here - build via CI (build-release.yml) or a local MinGW/zig toolchain and
  confirm line 2 of hp3mod.log says build v61.

  HARDWARE CHECK LIST for the user:
    * Log line 2 says build v61 (else an old DLL is being tested).
    * P1 solo vs a CompanionSpellTrigger: hold 10 s, release - the log shows
      the normal shot plus two "[virtual trio]" bonuses from Hermione/Ron
      positions, then Touch/ProcessTouch for the bonuses (delivery now ticks
      with split OFF), and the trigger completes.
    * P2 hold in a 2-player split: same three distinct origins/Instigators in
      the log, arrivals visibly staggered, trigger completes.
    * Ron during a P1 cooperative hold: walks to range unpinned, then holds
      still while casting ("casting held still (GS/AR pinned)"), and resumes
      following on unlock ("guard released ... (p1 unlock)"). No run/cast/run.
    * 10 s hold on a pumpkin/other object - NO trio, ordinary single cast.
    * Level travel mid-hold - no crash, no stuck pins (regression check).

19. v62 holder-origin virtual-trio correction (2026-09-09)

  Hardware testing confirmed that v61's distinct-Instigator triple cast works,
  but exposed a trajectory flaw: shots launched from remote companion positions
  can hit nearby walls and never reach the holder's target. The companion must
  contribute identity to CompanionSpellTrigger, not projectile geometry.

  v62 makes the player who held for 10+ seconds authoritative for the entire
  volley. P2/P3 bonus actors copy the ordinary holder projectile's exact
  Location, Velocity, Rotation, DesiredRotation, Acceleration, Speed, MaxSpeed
  and Physics before the next engine tick. P1's ordinary projectile remains
  engine-owned, so bonus #2 uses a holder-origin target-directed fallback and
  bonus #3 clones #2. The other two heroes are used only as Instigator labels;
  their positions and gameplay state are untouched. The same slot rotation
  applies symmetrically when P1, P2, or P3 is the holder.

  Hardware check: confirm hp3mod.log says build v62, spread all three heroes far
  apart, then perform a >10-second release once with each player. Every log must
  show both bonuses "at HOLDER origin" with matching location/velocity, and all
  three visible spells must follow the holder's line rather than remote walls.

20. v63 8-second arm + three floating wet spells (2026-09-09)

  User request: triple cast works exactly as intended. Two changes only.
    (1) Arm the charged x3 after an 8-second continuous hold (was 10).
    (2) Show the stock cooperative visual - the one floating wet-shader spell
        that becomes three floating spells when the AI companions join - while
        a spell is held past the threshold on the correct triple-cast objects.
  Functional triple-cast firing must not change.

  (1) TIMING. hp3coop::DefaultHoldMs is now 8000; the shipped
      src/hp3mod.ini and bin/hp3mod.ini say CoopCastHoldMs=8000 and document
      "minimum/default 8000" (values below 8000 stay clamped). The dllmain
      clamp already bottoms at hp3coop::DefaultHoldMs, so a sampled ini cannot
      undercut the new floor. coop_cast_test.cpp now derives every boundary
      from H = DefaultHoldMs, so the "exactly the interval is still a normal
      hold; Ready only after it fully elapses" rule is verified at 8000.

  (2) THREE FLOATING SPELLS. v59-v62 keep the holder's own floating gesture
      (engine cursor for P1, native-aim LOCK for P2/P3) but never borrow
      companions, so a mod-driven P2/P3 charged hold showed only ONE floating
      spell. v63 adds a coopTrio overlay: while g_coopActive is armed by a
      P2/P3 holder, two companion SpellGesture actors are spawned next to the
      cooperative target and driven with the same reflected ABI as the native
      aim LOCK gesture (spawnFX + nativeSetPhysics(0) + naPlace + ChangeGesture
      with the resolved <spell>.default.SpellIcon wet glyph + SetReadyToCast
      with the target collision radius). The holder's own gesture stays put,
      so the armed charge reads as three floating spells - exactly the stock
      1-to-3 look.

      * P1 is deliberately excluded: the stock cursor lock already recruits
        the AI companions and produces the same 1-to-3 look, so overlaying
        there would double the companion gestures.
      * Purely visual. The overlay owns only its two gesture actors and never
        writes companion controllers, cast state, target/spell fields, or AI;
        coopRestore's fire path is byte-for-byte untouched.
      * Lifecycle: armed lazily on the first coopMaintain frame after
        coopStart; refreshed every frame the charge stays alive; disarmed in
        coopRestore (release AND cancel), coopClearAll, and defensively in
        cgResetLevel. Liveness is re-checked through the object table before
        any write/destroy (level travel can free the actors between frames).
      * Availability gates: NativeAim on, reflected SpellGesture ABI present,
        and a resolvable wet icon. If any is missing the overlay is skipped
        without touching the firing path (the holder's own gesture would be
        missing too, since it needs the same machinery).

  Preserved unchanged: v62 holder-origin virtual-trio launch, v61
  distinct-Instigator virtual trio and split-OFF delivery tick, the
  state-aware companion movement guard, v58 level-travel safety and the
  cooperative-family gate, and the native wet SpellGesture icon / 770-unit
  aim. Nothing auto-fires at 8 seconds; x3 fires once on release.

  Host policy suites pass; the 32-bit Windows DLL cross-build succeeds with
  the v63 banner and Direct3DCreate8/Direct3DCreate8@4 exports; bin/d3d8.dll
  is refreshed to match. In-game behaviour still needs hardware confirmation.

  HARDWARE CHECK LIST for the user:
    * Log line 2 says build v63 (else an old DLL is being tested).
    * Hold a CompanionSpellTrigger as P2/P3 for just over 8 seconds: the
      holder's one floating wet spell must be joined by two more (three
      floating spells), and the log shows "[cooptrio] P2 charge armed ...
      three floating spells". Nothing fires at 8 seconds.
    * Release: the v62 holder-origin volley fires once (bonuses "at HOLDER
      origin"); the three floating spells disappear with the release.
    * 8-second hold on a pumpkin/spawner: NO trio, NO companion gestures,
      ordinary single cast.
    * A hold shorter than 8 seconds must not arm and must not show the trio.
    * Level travel mid-hold: no crash, no orphaned companion gestures.

21. V64 STOCK COOP GESTURE MOTION - FLOATING IN THE AIR (2026-09-09)
-------------------------------------------------------------------------------
- User request: "triple cast works alongside with triple wet shader animation,
  but I want this animation to be improved. Right now it seems to just spawn 2
  additional spell icons on side, but I want it to exactly replicate original
  main character coop cast behaviour - when you hold a cast and AI companions
  join, it shows floating wet shader spell icon moving a bit in air, not just
  locked. Find how main character does it with companions, compare to existing
  triple cast and replicate."

- HOW THE ORIGINAL MOVES THE GESTURE (HP2 KWGame source, same family; HP3
  docs table: "Gesture placement ... The original cursor also smooths its
  motion."):
    * SpellCursor.LockOn does NOT leave the gesture where it spawned: it
      snaps it once with SpellGesture.SetLocation(vLOS_End) (in front of the
      target at 1.1*CollisionRadius*SizeModifier + 2 + GestureDistance).
    * stateLockedOn.Tick then chases every frame with
      SpellGesture.MoveSmooth((goal - SpellGesture.Location) * rate * dt):
      rate 10 while the target is still the current possible target
      (goal = vLOS_End, which moves with the camera ray), rate 8 on the
      centre-offset goal (target.Location + CentreOffset +
      Normal(heroLoc - targetLoc) * dist) during a target flicker. So the
      icon is never "locked": it glides after the aim/its hero.
    * UnLock just hides the gesture; the same actor is reused.

- WHAT V63 DID WRONG: coopTrioUpdate spawned the two companion SpellGesture
  actors at centre +/- lateral spacing AT THE TARGET PLANE (z+6), never moved
  them after spawn, and destroyed/re-created them on any target-candidate
  flicker. The native-aim LOCK gesture was also SetLocation-ed at its goal
  every frame (a teleport). Result: three icons, two "on side", all pinned.

- V64 FIX (src/coop_trio.h + dllmain coopTrioUpdate + native_aim.h):
    * New pure policy header src/coop_trio.h: stock per-hero goal
      (heroGoal), front-of-target point (frontPoint), the split-screen
      cluster goal (clusterGoal = front point + hero direction projected
      perpendicular to the pane's view * v63 spacing), MoveSmooth chase
      semantics, facing yaw, lateral fallback. Host tests in
      tests/coop_trio_test.cpp (added to tests/run.sh).
    * Companion goals now = stock front point pushed sideways along each
      hero's own line of sight, so the three icons float in FRONT of the
      target on their heroes' sides, not at the target plane; live hero
      positions are read every frame (getPawn(hero slot)), so the goals move
      with the heroes.
    * Every icon (holder's native LOCK + two companions) is chased with
      hp3trio::moveSmooth((goal-loc) * 10 * dt), snapped once at
      spawn/lock exactly like LockOn.SetLocation; dt from GetTickCount,
      clamped to kMaxDt (0.05 s).
    * Target flicker: while coopActiveCandidate() is NULL, the overlay keeps
      its last centre/dist (t.centre/t.dist) and keeps gliding on the last
      goals - matching the stock lock, which survives aCurrentTarget
      flicker. Only a real target loss (outside the 250 ms grace, handled by
      coopRestore) or missing cached geometry disarms.
    * Fallback: lateralGoal(front, holderLoc, radius, side) (the v63 recipe,
      now anchored to the front point) only when a companion pawn is
      unavailable/stale.

- VERIFICATION: purity/motion tests pass on the host (coop_trio + native_aim
  suite); the x86 Windows DLL cross-build succeeds (Zig 0.13 in this
  sandbox; CI builds with 32-bit MinGW) with the v64 banner and
  Direct3DCreate8/Direct3DCreate8@4 exports. bin/d3d8.dll is refreshed.

  HARDWARE CHECK LIST for the user:
    * Log line 2 says build v64 (else an old DLL is being tested).
    * Hold a CompanionSpellTrigger as P2/P3 past 8 s: THREE wet icons now
      hover together in front of the target (not two beside it), and when
      you nudge the aim/camera or a hero moves, the icons GLIDE - a short
      damped drift, never a jump; on the first lock they appear in place
      (stock snap), after that they always ease.
    * Quick aim wiggle during the hold: the holder's icon trails the aim
      smoothly; the two companion icons drift with their heroes' sides.
    * Release: v62 holder-origin volley fires once; icons vanish.
    * A pumpkins/spawner hold still produces no trio and no gestures.

22. V65 EXACT STOCK PER-HERO GESTURE PLACEMENT (2026-09-09)
-------------------------------------------------------------------------------
- User report on v64: the central locked spell icon is correct, but the two
  companion icons that should flank it are "flying broken, overlapping one on
  another". Wanted: find the original coop cast spell-shader behaviour and
  replicate it exactly for P2/P3 - three wet shader spells floating together.

- ROOT CAUSE (what v64 did wrong): coopTrioUpdate anchored BOTH companion
  gestures at the HOLDER's camera front point (centre - viewDir * dist) and
  then pushed each sideways by a small fixed spacing (radius*0.9+25, clamped
  50..120) along the hero's direction PROJECTED perpendicular to the holder's
  view (clusterGoal). The two companion heroes trail together, so their
  target->hero directions are nearly parallel; their projections collapse onto
  nearly the same perpendicular axis (or to zero when a hero sits on the view
  axis), and both icons chase nearly the same goal - hence the overlap. The
  v63 lateral fallback (lateralGoal) was the same idea anchored to the front
  point.

- WHAT THE ORIGINAL ACTUALLY DOES (HP2 KWGame SpellCursor source, same family;
  HP3 package gesture-placement rules in docs/NATIVE_AIM.md): every joined
  hero owns ONE SpellGesture, driven by that hero's own cursor lock. Its goal
  is that hero's OWN vLOS_End:
      target.Location + CentreOffset +
        Normal(heroLoc - targetLoc) *
          (1.1 * CollisionRadius * SizeModifier + 2 + GestureDistance)
  i.e. the point at the stock gesture distance on the hero's own line of sight.
  SpellCursor.LockOn snaps it there once (SetLocation), then stateLockedOn.Tick
  chases every frame with SpellGesture.MoveSmooth((goal-loc)*rate*dt) - rate
  10 while the target is still the current possible target, rate 8 on the
  centre-offset goal during a target flicker. The gesture faces opposite its
  owner's line of sight (target -> hero). Because the three heroes' lines fan
  out from the target, the three icons naturally sit apart on their own
  heroes' sides - no artificial spacing is needed or used.

- V65 FIX (src/coop_trio.h + dllmain coopTrioUpdate):
    * coop_trio.h now contains ONLY the stock policy: direction(), heroGoal()
      (the per-hero vLOS_End above), facingYaw(), and moveSmooth(). The
      non-stock frontPoint()/clusterGoal()/lateralGoal() helpers are removed.
    * Each companion gesture's goal is hp3trio::heroGoal(centre, heroLoc, dist)
      with the LIVE hero location read every frame (getPawn(hero slot)); its
      facing is hp3trio::facingYaw(Normal(heroLoc - centre)) (target -> hero),
      its own yaw, not the shared pane-viewer yaw.
    * MoveSmooth chase is unchanged but now uses the stock rate selection:
      kSmoothRate (10) while coopActiveCandidate() is live, kSmoothRateFlicker
      (8) while the target candidate flickers (cached centre) - matching the
      stock "rate 10 locked / rate 8 flicker" rule. Snapped once at lock.
    * A companion hero unavailable (or exactly at the aim centre) for a frame
      keeps gliding toward its last goal (t.goal/t.facing cached per
      companion); the previous "lateral fallback" fabrication is gone. No
      cached goal at all disarms the overlay (nothing stock-consistent to
      draw).
    * The holder's native-aim LOCK gesture is untouched (the user confirmed
      the central icon is correct).

- VERIFICATION: tests/coop_trio_test.cpp now asserts the stock per-hero goals
  (two companions on opposite sides -> distinct goals; same-side companions ->
  still fanned apart), the facing-yaw convention, and MoveSmooth snap/chase at
  both rates 10 and 8. Host policy suites pass. No Windows cross toolchain
  exists in this sandbox, so dist/d3d8.dll and bin/d3d8.dll could NOT be
  rebuilt here - build via CI (build-release.yml, workflow_dispatch) and
  confirm line 2 of hp3mod.log says build v65.

  HARDWARE CHECK LIST for the user:
    * Log line 2 says build v65 (else an old DLL is being tested).
    * Hold a CompanionSpellTrigger as P2/P3 past 8 s: the holder's central wet
      icon is unchanged, and the two companion icons now sit apart on their
      OWN heroes' sides of the target (the heroes' real positions fan the
      icons out), never overlapping one another.
    * Move a hero (or let the AI reposition them) during the hold: each
      companion icon glides after its own hero; the icons re-fan as the
      heroes move apart and only come together if the heroes themselves
      stand together (the exact original behaviour).
    * Quick aim wiggle: the holder's icon trails the aim smoothly; the
      companions keep following their heroes, rate 8 only during a target
      flicker.
    * Release: v62 holder-origin volley fires once; icons vanish.
    * A pumpkins/spawner hold still produces no trio and no gestures.

23. V66 SPONGIFY AIR YAW ROTATION UNLOCK & LUMOS SECRET WALL PASSABILITY (2026-09-09)
-------------------------------------------------------------------------------
- User request:
  1. Fix Spongify view rotation lock where camera is restricted to vertical
     movement only (horizontal/yaw rotation locked).
  2. Research Lumos pass-through mechanics on see-through walls (temporary
     state/timer on main character vs wall collision) and replicate the
     mechanic on other player/companion characters so all players can walk
     through revealed walls.

- ROOT CAUSE 1 (SPONGIFY / JUMP VIEW YAW LOCK):
  In src/dllmain.cpp (driveePawn), during standing jumps or Spongify pad bounces
  (g_jumpStanding[i] == TRUE while airborne in PHYS_Falling, phNowLock == 2),
  g_viewYaw[i] was continuously overwritten each frame with g_jumpYawLock[i].
  While vertical pitch (g_lookPitch[i]) updated freely from stick/mouse input,
  horizontal yaw rotation was wiped out every frame.
  Fix: Remove the mid-air g_viewYaw[i] = g_jumpYawLock[i] assignment in
  driveePawn. The companion's DesiredRotation continues to track g_viewYaw[i]
  with RotationRate = 0 so the companion AI cannot pull the pawn toward stale
  waypoints in mid-air, while rot[1] = g_viewYaw[i] keeps the mesh facing the
  player's camera view. Horizontal and vertical camera/aim rotation are now
  both fully free during Spongify launches and jumps.

- ROOT CAUSE 2 (LUMOS & SEE-THROUGH SECRET WALL PASSABILITY):
  In single-player HP3, casting Lumos on a gargoyle attaches and activates
  TheLumosLight (a LumosLight actor) on the hero's wand (harry.Wand.TheLumosLight)
  and starts a temporary Lumos timer. LumosSparklesTrigger / LumosTrigger at secret
  walls detect the proximity/touch of TheLumosLight (or active Lumos wand) and
  manage transparency and collision for GenericColObj / KWBlockingVolume.
  In splitscreen:
  1. When a companion (P2/P3) casts Lumos on a gargoyle or when Lumos is
     activated, companion wands did not have TheLumosLight active/unhidden.
  2. When companions approached the secret wall without Lumos on their own wand,
     LumosSparklesTrigger never detected Lumos nearby.
  3. GenericColObj / KWBlockingVolume retained companion-blocking collision.
  Fix (src/lumos_sync.h + dllmain.cpp):
  1. Pure state policy header src/lumos_sync.h tracking active state, timer/duration
     (DefaultLumosDurationMs = 25s), light property verification, cylinder
     proximity detection, and secret wall passability.
  2. In cgDeliverHit, hitting a gargoyle or casting Lumos activates the Lumos
     state.
  3. In lumosTick (called every frame), active Lumos state is synchronized across
     all player pawns and wands: TheLumosLight is activated (LT_Steady, brightness
     255, collision enabled) and ShowWeapon is called.
  4. Nearby LumosSparklesTrigger / LumosTrigger are triggered for any player
     carrying Lumos, and GenericColObj / KWBlockingVolume collision is unblocked
     (bCollideActors / bBlockPlayers = 0) so all players can walk seamlessly
     through revealed see-through walls.

- VERIFICATION: tests/lumos_sync_test.cpp asserts state activation, expiration,
  tick-wrapping safety, light property checks, cylinder proximity detection,
  and passability rules. All test suites pass via ./tests/run.sh.

24. V66.1 PERF REGRESSION & STUCK COMPANION (2026-09-09)
--------------------------------------------------------
- User report after v66 (with the pasted game log as evidence):
  1. Frame rate collapsed after updating to v66 - even the MENU is laggy.
     The log's own perf counter reads single digits throughout
     ("[perf] 2.77 fps", "... 7.00 fps ...") at 3440x1440.
  2. Hermione as Player 2 gets STUCK after entering Hogwarts
     ('Save0' -> 'HP3_InsideHub'): the log shows her frozen at
     loc=(974 1063 Z=137) vz=0 phys=2, "jump ignored (airborne, phys=2)"
     forever, while her forward/turn/look input is clearly present.

- ROOT CAUSE 1 (FPS): v66's lumosTick (src/dllmain.cpp) ran every rendered
  frame on BOTH the split-ON and split-OFF paths (the split-OFF path returns
  after calling it, so it also ran in the menu). Each call performed:
    * getPawnWand() + getWandLumosLight() for every player; the
      TheLumosLight property offset could not be resolved by name
      (log: "TheLumosLight=+0xFFFFFFFF"), so getWandLumosLight fell back to
      a FULL GObjObjects scan calling GetFullName (objName) on every object,
      every frame, per player.
    * A second FULL GObjObjects scan with objName on every object to find
      LumosSparklesTrigger / LumosTrigger / GenericColObj / KWBlockingVolume
      by substring - every frame.
  GetFullName walks the Outer chain and formats a string; ~9k objects in
  HP3_InsideHub x several scans per frame collapsed the frame rate to single
  digits. This is a v66 addition (the lumos replication), hence the "even the
  menu is laggy" symptom (the menu runs the same PostRender path).

  FIX: cache everything the scan used to re-find each frame.
    * g_lumosTriggers[] / g_lumosWalls[] hold the matched actors; the full
      table scan runs ONCE PER LEVEL (lumosScanActors), re-armed only when a
      representative cached pointer stops being live (level-change race) or on
      explicit invalidation.
    * lumosInvalidate() clears the caches and is called from
      invalidateLevelCaches() on every level travel, so cached pointers never
      outlive their level (v57 crash discipline).
    * g_lumosWand[]/g_lumosLight[] cache the per-player wand/light; the
      wand-table fallback is throttled to 1 Hz and the light-table fallback to
      1 Hz (the direct TheLumosLight offset read stays per-frame when it can
      be resolved).
    * Trigger fire is edge-triggered (g_lumosTrigFired[]) so a held Lumos no
      longer calls Trigger() every frame while near a secret wall.
  Per-frame cost drops from several full GetFullName scans to a handful of
  liveness checks + proximity tests over a small cached list.

- ROOT CAUSE 2 (STUCK HERMIONE): after the Hogwarts door transition the driven
  companion is parked in PHYS_Falling (phys=2) with NO real fall - Location Z
  stays flat at 137 and Velocity.z stays 0 (the engine's falling gravity never
  runs on her). driveePawn's movement branch only promoted PHYS_None(0) ->
  PHYS_Walking(1); a pawn already in PHYS_Falling(2) was left in place, so
  forward input kept writing XY velocity while the engine kept her airborne
  and every jump was refused ("grounded jumps only").

  FIX (driveePawn): before the idle/move split, detect a stranded pawn -
  phys == 2, no jump in flight (g_jumpPh == 0, !g_jumpStanding), |vz| < 60,
  and a valid Location - and require that Z stay flat (within 8 uu) for a
  600 ms grace window (a genuine jump or ledge fall moves Z and resets the
  timer). Then hand the engine PHYS_Walking back through nativeSetPhysics(pawn,
  1) (which re-initialises Base/Floor bookkeeping that a raw byte write would
  skip) and g_FindBase() to re-acquire the floor. Logs "[physrecover] pN
  stranded Falling->Walking".

- VERIFICATION: host policy suites pass (tests/run.sh). The Windows DLL could
  NOT be rebuilt in this sandbox (no i686 MinGW toolchain; package install
  blocked), so dist/d3d8.dll and bin/d3d8.dll are NOT refreshed here - build
  via CI (build-release.yml, workflow_dispatch) and confirm line 2 of
  hp3mod.log says build v66.1.

  HARDWARE CHECK LIST for the user:
    * Log line 2 says build v66.1 (else an old DLL is being tested).
    * Menu + field frame rate is back to normal (no more single-digit [perf]).
    * After entering Hogwarts as P1 while split is ON, Hermione moves again;
      the log shows "[physrecover] p1 stranded Falling->Walking" if she ever
      parks in mid-air.

==========================================================================
2026-09-09 (evening) - v66.3: LUMOS AS PLAYER 2 GPF (Texture receiver)
==========================================================================

- FIELD REPORT: casting Lumos as Player 2 (Hermione, driven pawn index p1)
  crashed the game with the Windows fault dialog:

    History: UObject::ProcessEvent <- (Texture hgame.LumosTriggerIcon,
             Function KWGame.KWPawn.Trigger) <- FPlayerSceneNode::Render
             <- UGameEngine::Draw <- UWindowsViewport::Repaint <- ...

  hp3mod.log ends one ProcessEvent earlier: "-> ProcessEvent
  LumosTrigger.Trigger" right after "[lumos] p1 delivered Lumos hit to
  gargoyle HP3_InsideHub.gargoyle6 - Lumos activated for 25s".

- ROOT CAUSE: the v66 per-level Lumos scan (lumosScanActors) admitted ANY
  object whose whole GetFullName string ("<Class> <Package>.<ObjectName>")
  contained "LumosTrigger"/"LumosSparklesTrigger"/"LumosSparkles". Besides
  the placed trigger actors that also means:

    * the UClass objects themselves: "Class hgame.LumosTrigger",
      "Class hgame.LumosSparklesTrigger";
    * the spell's HUD texture: "Texture hgame.LumosTriggerIcon" - loaded
      into the object table alongside Lumos.

  A cached Texture then passed every fire-time guard in lumosTick:
  cgLiveObject says live (it IS a live UObject), and the Location
  (+0x150) / CollisionRadius reads at ACTOR offsets land inside the shared
  UObject allocator arena, so IsBadReadPtr passes and garbage floats can
  satisfy the player-proximity cylinder. The trigger loop then called
  callFn(texture, g_fnTrigger="KWGame.KWPawn.Trigger") - pawn script
  bytecode executing with a texture-sized object as 'this'. Instant GPF,
  receiver named verbatim in the crash history. The GenericColObj /
  KWBlockingVolume secret-wall scan had the same flaw (its substring matched
  the wall classes themselves, which then got actor collision bits written
  into class memory).

- FIX (v66.3):
    * Scan: membership is decided by the object's CLASS TOKEN only (the word
      before the first space of the full name) - hp3lumos::isLumosTriggerObject /
      isSecretWallObject in src/lumos_sync.h. Rejected look-alikes are counted
      and logged once per scan ("[lumos] scan: ... name look-alikes rejected").
    * Fire/write sites (defense in depth): immediately before ProcessEvent
      and before any wall collision write, lumosClassVerified() re-checks the
      receiver through the calibrated UObject::Class chain (cgClassOf ->
      cgClassInfo, guarded dword reads only - no GetFullName, no Outer walk,
      safe on a recycled pointer). A proven non-actor/wrong-family object is
      skipped and logged once. While the class chain is not yet verified
      (fresh level, index not rebuilt), the strict scan classification stands.
      This also closes the pointer-recycling variant: a destroyed trigger's
      pool address being reused by a different object mid-level.

- VERIFICATION: host policy suites pass (tests/run.sh) - the lumos suite now
  replays the exact crash names ("Texture hgame.LumosTriggerIcon",
  "Class hgame.LumosTrigger", "LumosSpell HP3_InsideHub.LumosSpell1",
  "Class KWGame.KWBlockingVolume", ...) against the new classifiers. The
  Windows DLL could NOT be rebuilt in this sandbox (no i686 MinGW toolchain;
  package install blocked), so dist/d3d8.dll and bin/d3d8.dll are NOT
  refreshed here - build via CI (build-release.yml, workflow_dispatch) and
  confirm line 2 of hp3mod.log says build v66.3.

  HARDWARE CHECK LIST for the user:
    * Log line 2 says build v66.3 (else an old DLL is being tested).
    * The per-level line "[lumos] scan: N triggers, M walls cached; A+B name
      look-alikes rejected (v66.3 class-token rule)" appears once per level.
    * Casting Lumos as Player 2 (index p1) at the HP3_InsideHub gargoyle no
      longer crashes; "-> ProcessEvent LumosTrigger.Trigger" is followed by
      normal [pelog]/[castwatch] traffic instead of the detach line, and any
      rejected entry would show as "[lumos] v66.3 BLOCKED Trigger()".
    * Secret walls still open for every player while a Lumos light is
      carried (the real trigger/wall actors are unchanged by this fix).

==========================================================================
2026-09-09 (night) - v66.4: LUMOS AS PLAYER 2 GPF, SECOND RECEIVER
                     (LumosSparklesEmitter actor, with v66.3 running)
==========================================================================

- FIELD REPORT: with build v66.3 installed (its log banner confirms it),
  casting Lumos as Player 2 (Hermione, driven pawn index p1) at the SAME
  HP3_InsideHub gargoyle crashed again with the Windows fault dialog:

    History: UObject::ProcessEvent <- (LumosSparklesEmitter
             HP3_InsideHub.LumosSparklesEmitter0,
             Function KWGame.KWPawn.Trigger) <- FPlayerSceneNode::Render
             <- UGameEngine::Draw <- UWindowsViewport::Repaint <- ...

  hp3mod.log ends with the fire itself visible - the mod's own callFn lines
  interleaved with the pelog ProcessEvent hook output:

      -> ProcessEvent LumosTrigger.Trigger
  [pelog] @+1372ms LumosTrigger HP3_InsideHub.LumosTrigger0
         :: Function KWGame.KWPawn.Trigger
      -> ProcessEvent LumosTrigger.Trigger
  [pelog] @+1372ms LumosSparklesEmitter HP3_InsideHub.LumosSparklesEmitter0
         :: Function KWGame.KWPawn.Trigger
  === detach (frames=1151 postrender=1152) ===

  So this time the GPF receiver is not a stray texture but a real placed
  ACTOR of class hgame.LumosSparklesEmitter (the secret-wall sparkles
  effect emitter), fired on one cache slot after the genuine LumosTrigger0.

- ROOT CAUSE (two cooperating defects):

  1. v66.3 MOVED the substring match but did not REMOVE it. Its
     isLumosTriggerClassToken() substring-matched WITHIN the class token
     (strstr(clsTok, "LumosSparkles")), and the fire-time chain re-verify
     (lumosClassVerified) re-ran that same substring on the chain-derived
     class token behind an isActor gate. Class token "LumosSparklesEmitter"
     contains "LumosSparkles", and an Engine.Emitter IS an actor subclass -
     both gates green. The hub scan line in the field log shows the result:
     "[lumos] scan: 21 triggers, 80 walls cached; 62+18 name look-alikes
     rejected (v66.3 class-token rule)" - 21 cached "triggers" for a level
     whose placed Lumos trigger actors number far fewer (the same level's
     castgame scan lists two gargoyles and a handful of trigger triggers);
     the emitters filled the rest.

  2. The deeper defect, unchanged since v66: the trigger fire loop called
     ONE globally resolved function pointer (g_fnTrigger, resolved at init
     to "KWGame.KWPawn.Trigger") on every cached entry. ProcessEvent
     dispatches exactly the UFunction it is handed, with whatever 'this' it
     is handed - there is no receiver-class check - so any admitted
     non-pawn object executes pawn member-offset reads/writes with a
     too-small 'this'. (The genuine LumosTrigger0 merely survived the same
     misuse, which is why only the second fire in the pair above detaches.)
     Receiver AND function are both named verbatim in the crash history.

- FIX (v66.4, three layers - src/lumos_sync.h + src/dllmain.cpp):

    * Scan membership by name is now EXACT class-token equality, case
      insensitive (hp3lumos::tokenEquals): triggers = "LumosTrigger" /
      "LumosSparklesTrigger" only, walls = "GenericColObj" /
      "KWBlockingVolume" only. NO substring comparison remains on any Lumos
      gate. "LumosSparklesEmitter", "LumosTriggerLarge", "LumosSparkles",
      "LumosLight", icon/texture/class/function look-alikes all reject at
      the name test. The look-alike rejection counters and per-scan log
      line are kept (now "... (v66.4 exact-family rule)").

    * The only feature the substring ever bought - catching a
      differently-named SUBCLASS of one of those classes - is now served by
      proof instead of text: lumosChainProves() walks the receiver's
      calibrated UObject::Class -> UStruct::SuperField chain (cgClassOf /
      cgSuper: IsBadReadPtr-guarded dword reads, known-class validation, no
      GetFullName anywhere) and pointer-compares it against the family
      class objects resolved by path at init (hgame.LumosTrigger,
      hgame.LumosSparklesTrigger, KWGame.GenericColObj,
      KWGame.KWBlockingVolume - printed once in the log as "[lumos] family
      classes:"). A name can never fake a pointer in the chain. The proof
      runs at scan admission (for name look-alikes only, so the once-per
      level scan stays cheap) and inside lumosClassVerified at every
      fire/write. While the class chain is not yet verified on a fresh
      level, the strict exact-name scan result stands (the v66.3 documented
      trust level); a first scan that raced the chain calibration and left
      look-alikes unproven re-runs exactly once once the chain verifies
      (g_lumosRescanOnChain), so subclass admission cannot be lost to init
      order either.

    * ROOT-CAUSE HARDENING at the fire site: the mod no longer replays a
      function found on a DIFFERENT class onto any receiver. With the chain
      verified, the fire resolves the most-derived Trigger declaration in
      the RECEIVER's OWN class chain (CgClass::fnTrigger - the castgame
      index already maps every class-level "Trigger" function onto every
      class, most-derived first), and falls back to Engine.Actor.Trigger -
      the base Trigger event every actor owns - if the chain declares no
      override. If neither can be proven for the object in hand, the fire
      is skipped and logged ("[lumos] v66.4 BLOCKED Trigger() on %p - ...").
      Even a hypothetical FUTURE admission bug therefore cannot execute
      foreign bytecode on a live object again, and for real Lumos trigger
      actors this fires exactly what the engine's own touch path
      dispatches, so stock behaviour is unchanged. (The pawn-only
      doUse()/Trigger() path is untouched - its receivers are pawns, which
      is what g_fnTrigger was always correct for.)

- VERIFICATION: host policy suites pass (tests/run.sh); the lumos suite
  (tests/lumos_sync_test.cpp) replays BOTH crash receivers verbatim
  ("Texture hgame.LumosTriggerIcon", "LumosSparklesEmitter
  HP3_InsideHub.LumosSparklesEmitter0"), the previously-accepted token
  substrings that must now reject ("LumosSparkles", "LumosTriggerLarge"),
  case-insensitive exact acceptance of the four stock class tokens, and the
  chainContainsAny subclass-proof primitive (hit at chain position 0 and
  deep, wall family with an unresolved second ref, and all NULL/empty guard
  rails). The Windows DLL could NOT be rebuilt in this sandbox (no i686
  MinGW toolchain; package install blocked), so dist/d3d8.dll and
  bin/d3d8.dll are NOT refreshed here - build via CI (build-release.yml,
  workflow_dispatch) and confirm line 2 of hp3mod.log says build v66.4.

  HARDWARE CHECK LIST for the user:
    * Log line 2 says build v66.4 (else an old DLL is being tested).
    * "[lumos] family classes: LumosTrigger=........ LumosSparklesTrigger=
      ........ GenericColObj=........ KWBlockingVolume=........" prints
      non-NULL pointers once per session.
    * The once-per-level scan line reads "[lumos] scan: N triggers, M walls
      cached; A+B name look-alikes rejected, C+D subclasses chain-admitted
      (v66.4 exact-family rule)"; in HP3_InsideHub N drops well below the
      bogus 21 (the emitters are no longer cached - they only ever needed
      to be triggered BY the real triggers' own scripts).
    * Casting Lumos as Player 2 at the HP3_InsideHub gargoyle: the
      "-> ProcessEvent LumosTrigger.Trigger" line is followed by normal
      [pelog]/[castwatch] traffic instead of the detach line, and nothing
      named "LumosSparklesEmitter" is ever the receiver of that call.
    * Any surviving anomaly shows up as "[lumos] v66.4 BLOCKED Trigger()".
    * Secret walls still open for every player while a Lumos light is
      carried (the real trigger/wall actors are unchanged by this fix).

==========================================================================
2026-09-09 (late) - v66.5: LUMOS AS P2 - WALL STAYS SOLID + QUIT CRASH +
                     P2's WAND VANISHES (one root cause + two satellites)
==========================================================================

- FIELD REPORT: with build v66.4 installed, casting Lumos as Player 2 at
  the HP3_InsideHub gargoyle now SURVIVES (the v66.3/v66.4 crashes are
  gone) and the spell itself works - the gargoyle lights, the mod's state
  engages for its 25s window - but:

    (a) P2's wand mesh disappears for the whole Lumos window ("the wand
        disappears"), and returns afterwards;
    (b) the revealed secret wall stays solid - "spell works, but I still
        can't go through wall, it doesn't seem to apply correct state";
    (c) quitting the game crashed with

    Assertion failed: Actor->bCollideActors [File:UnOctree.cpp] [Line: 1598]
    History: FCollisionOctree::RemoveActor <- FOctreeNode::RemoveAllActors
             <- FCollisionOctree <- ULevel::Destroy <-
             UObject::ConditionalDestroy <- DispatchDestroy <-
             PurgeGarbage <- StaticExit <- appPreExit

  The game log also shows TheLumosLight=+0xFFFFFFFF (the wand's light
  property offset never resolved) and the family classes
  LumosTrigger/LumosSparklesTrigger/KWBlockingVolume=NULL (only
  GenericColObj resolved) - the chain-proof path was inert for 3 of the 4
  families, exactly as v66.4 predicted it might be on this build.

- ROOT CAUSE (b)+(c), one defect: the wall replication wrote the
  bCollideActors/bBlockActors/bBlockPlayers BITS straight into the cached
  wall actors (setLumosActorCollision). Collision in this engine is
  resolved through the collision OCTREE, and only the native
  Engine.Actor.SetCollision ever adds or removes an actor from it - a
  poked bit changes the flag, not the collision. So (b): the "opened"
  wall stayed in the octree and kept blocking every pawn that walked into
  it (HP2-lineage ground truth: KWBlockingVolume/GenericColObj are just
  actors; their empty Trigger()/Touch()/Bump() overrides prove no script
  path re-syncs flags with the octree either). And (c): at quit, the
  octree's destructor walked its still-present members and tripped
  check(Actor->bCollideActors) on a member whose flag the mod had cleared
  - the exact UnOctree.cpp:1598 assertion. Bit-poking was wrong in BOTH
  directions at once: it could not open the wall, and it corrupted the
  octree/flag invariant that engine teardown checks.

  (a) was a satellite defect: while Lumos was active the [lumos] sync
  called hgame.HPCharacter.ShowWeapon on EVERY pawn EVERY frame through
  callFn's ZEROED parms block. In the HP2 lineage ShowWeapon/HideWeapon
  are the cutscene commands "Weapon.bHidden = False/True" (harry.uc) - so
  a ShowWeapon(bool) handed zeros is a HIDE. The mod was hiding P2's
  freshly cast wand every frame. (The same call at cast-begin is harmless
  because StartCasting/SwitchToFightStanceAnims re-show the wand right
  after.)

  A second satellite explains "doesn't seem to apply correct state": the
  per-wand light replication never found any light. Stock
  baseWand.PostBeginPlay spawns TheLumosLight as Spawn(Class'LumosLight',
  self /* the wand */, Location) - the wand is the light's OWNER - and the
  wand's Tick only SetLocation()s the light to the wand end point
  (PHYS_None, never SetBase'd on anything). The v66.x fallback matched
  Base == wand/pawn, which is always false for this actor; with
  TheLumosLight=+0xFFFFFFFF the direct property read was gone too, so the
  whole light sync (and the light-driven state machine feeding it) was a
  silent no-op.

- FIXES (all in this build):

    * EVERY wall transition now goes through the engine's own native.
      execSetCollision ("?execSetCollision@AActor@@QAEXAAUFFrame@@QAX@Z")
      is resolved from Engine.dll exactly like execSpawn/execSetPhysics/
      execSetLocation, and driven with synthesized OP_TRUE/OP_FALSE
      bytecode tokens - the pattern the DrawPortal call already uses for
      its bool parm (nativeSetCollision, src/dllmain.cpp). An opened wall
      is genuinely REMOVED from the collision octree: passable for real,
      for every player. The octree/flag pair stays consistent at every
      instant - that consistency is what removes the quit crash even if
      the game is closed mid-spell.

    * Pristine per-wall collision bits are snapshotted ONCE per level at
      scan time (readLumosCollisionBits -> hp3lumos::packWallBits), and a
      close restores exactly those bits through the same native.
      lumosInvalidate now calls lumosRestoreAllWalls BEFORE dropping the
      caches, so level travel hands every still-open wall back its stock
      collision state while the pointers are still valid.

    * Only SECRET walls are ever opened: a wall is a candidate only when a
      cached LumosTrigger/LumosSparklesTrigger sits within
      SecretWallPairRadius (900 units) of it at scan time (pairing counted
      and logged per scan). v66.x opened ANY GenericColObj/KWBlockingVolume
      within 300 units of ANY player while Lumos was on - all 80 cached
      proxies in HP3_InsideHub were in reach. The open policy itself is
      hp3lumos::wallShouldBeOpen(lumosActive, secretCandidate, playerNear)
      in src/lumos_sync.h, regression-tested.

    * If execSetCollision is not exported, the wall feature disables
      itself and logs ONCE ("secret-wall passability unavailable") - a
      wall that cannot be opened properly stays solid. Collision bits are
      never poked again, anywhere; setLumosActorCollision is deleted.

    * The ShowWeapon spam is gone. While Lumos is active the mod writes
      bHidden=false directly on each pawn's Wand actor - the stock
      ShowWeapon command's exact effect, one dword write, idempotent,
      signature-independent. No native call, no zeroed parms, and the
      driven companion visibly carries the lit wand.

    * The wand-light lookup actually finds lights now: exact-class
      LumosLight (pointer compare against the resolved hgame.LumosLight
      class object - no GetFullName) + Engine.Actor.Owner == wand. The
      cached light is re-validated against the CURRENT wand by reading its
      Owner back (one guarded dword read), so a wand swap is picked up
      immediately and the 1 Hz owner-scan only runs when the cache misses.
      While active the light gets the stock LumosLight.TurnDynamicLightOn
      values (LT_Steady, brightness 255, radius 15, hue 32, saturation
      72), and its own collision is deliberately NEVER touched (stock
      spawns it SetCollision(False,False,False); LumosLight.Touch is an
      empty no-op; the old setLumosActorCollision(light, TRUE) poke is
      gone with the writer).

    * The timer FOLLOWS a genuinely burning light: while any wand light
      is on (isLightActive), the state refreshes in 1-second extend-only
      ratchets (State::refresh, FollowLightRefreshMs). An infinite-Lumos
      gargoyle keeps secret walls openable for exactly as long as its
      light burns; the state dies within a second of the last light. The
      ratchet comparison is tick-wrap safe (the same signed-difference
      test isExpired uses - a plain > across the 32-bit wrap could move
      the expiry BACKWARDS; caught by the new test section).

    * The dead Trigger() fire loop is REMOVED. The v66.4 hardware log
      proved the most-derived Trigger on the placed LumosTriggers is
      Engine.Actor.Trigger - the empty base event (KWGame sources confirm
      the same: KWBlockingVolume.Trigger() is empty; in the HP2 lineage
      LumosTrigger/LumosSparkles are OnLumosOn()/OnLumosOff() state
      machines that the stock LumosLight.TurnOn AllActors broadcast arms
      when the gargoyle lights). Firing it was a verified no-op; the
      stock chain stays untouched and the mod's any-player replication is
      the octree-safe SetCollision loop. The trigger cache stays - it is
      what marks walls as secret walls.

- VERIFICATION: host policy suites pass (tests/run.sh). The lumos suite
  (tests/lumos_sync_test.cpp) adds section 11: the wallShouldBeOpen truth
  table (8 cases - the secretCandidate gate is the v66.5 delta), the
  pack/unpack roundtrip over all 8 collision-bit combinations plus the
  WallBitsUnknown sentinel (never equal to a legal pack result), and the
  State::refresh extend-only ratchet (shortening proposals rejected,
  chained 1s ratchets track a burning light for minutes and expire within
  one FollowLightRefreshMs of the last refresh, wrap-crossing refresh
  extends correctly). The Windows DLL could NOT be rebuilt in this
  sandbox (no i686 MinGW toolchain; package installs blocked), so
  dist/d3d8.dll and bin/d3d8.dll are NOT refreshed here - build via CI
  (build-release.yml, workflow_dispatch) and confirm line 2 of hp3mod.log
  says build v66.5. The v66.5-edited functions (nativeSetCollision,
  readLumosCollisionBits, getWandLumosLight, lumosLightOf,
  lumosRestoreAllWalls, lumosInvalidate, lumosScanActors, lumosTick) were
  additionally compile-checked on the host against stubbed engine
  surfaces (g++ -fsyntax-only -Wall: clean).

  HARDWARE CHECK LIST for the user:
    * Log line 2 says build v66.5 (else an old DLL is being tested).
    * "execSetCollision = ........ (v66.5 lumos secret walls)" prints a
      non-NULL pointer at startup. If it is NULL the build is unusable
      for this feature - report the Engine.dll version.
    * The scan line now ends "... (v66.4 exact-family rule); N walls
      paired to a lumos trigger (v66.5 secret-wall candidates)". N >= 1
      in a level with a Lumos secret wall.
    * Cast Lumos as Player 2 at the HP3_InsideHub gargoyle: P2's wand
      STAYS OUT and lit for the whole window; walking ANY player into the
      revealed wall passes through it (expect "[lumos] v66.5 opened
      secret wall #k (SetCollision off - removed from the collision
      octree...)" in the log once per wall); backing away closes it
      ("closed secret wall #k (pristine collision bits restored)").
    * Let the Lumos window expire, or keep it burning and quit the game
      mid-spell: NO UnOctree.cpp assertion, no crash dialog on exit.
    * Level travel while a wall is open restores it (no assertion there
      either).

==========================================================================
25. V67 - WHAT STOCK LUMOS REALLY DOES, REPLICATED FOR P2/P3 (2026-09-10)
==========================================================================

- FIELD REPORT (v66.5, HP3_InsideHub gargoyle): Lumos as Player 2 no longer
  crashes and the gargoyle lights, but P2/P3 wands never glow and they still
  cannot walk through the revealed secret walls ("the game doesn't seem to
  give them that state").

- THE MISSING ANALYSIS (the user asked: "analyze what Lumos does to main
  character and replicate to other players"). The decompiled HP2-lineage
  scripts (github.com/metallicafan212/HP2UScriptDecompile - HP3 inherits the
  classes verbatim; the HP3 save chain
  "harry.Wand -> HarryWand.TheLumosLight -> LumosLight" is visible in public
  save-error dumps) give the whole stock chain:

    gargoyle.HandleSpellLumos(spell, hitLoc)
      -> baseWand(PlayerHarry.Weapon).LumosTurnOn()
      -> TheLumosLight.TurnOn():
           fLumosTime = 0;
           if (PlayerHarry.bLumosOn) return;      <-- single-player gate
           bLumosOn = True; PlayerHarry.bLumosOn = True; Enable('Tick');
           TurnDynamicLightOn();                  LT_Steady, LE_NonIncidence,
                                                  brightness 400, hue 32,
                                                  saturation 72, radius 15,
                                                  radiusInner 5
           foreach AllActors(Actor, A) A.OnLumosOn();  arms every trigger
           Particles = Spawn(Class'LumosLightFX', self, , Location);
                                                  <-- THE visible wand glow
    baseWand.Tick(): while TheLumosLight.bLumosOn:
           TheLumosLight.UpdateLocation(WandEndPoint)  light+particles ride
                                                       the wand tip
    LumosLight.Tick(): after fLumosTimeToTurnOff (30s) -> TurnOff():
           PlayerHarry.bLumosOn = False; TurnDynamicLightOff();
           foreach AllActors -> A.OnLumosOff(); Particles.Destroy();

    LumosTrigger (one placed at every secret wall):
      OnLumosOn() -> StateWaitingToTurnOn
      Tick: VSize(Location - PlayerHarry.Location) < fDistanceCheck(512)
              [optional |dZ| < fZDistanceCheck(64)]
            -> StateLumosOn -> BeginState:
                 TriggerEvent(Event, self, None)   <-- OPENS THE WALL
                 bFirstEventSent = True            (NEVER reset: fires once
                                                    per level; stock walls
                                                    are Movers, initial
                                                    state TriggerToggle,
                                                    MoveTime=0, so one fire
                                                    = open permanently)

  THREE splitscreen gaps fall straight out of that source:
    1. HandleSpellLumos lights PlayerHarry's wand even when P2 casts - a
       companion NEVER gets a lit wand from the stock chain.
    2. The wand glow is a spawned LumosLightFX particle actor, not a light
       property - poking light values (v66.x) can never produce the glow.
    3. The wall-open path is the trigger's Event, keyed on PlayerHarry's
       position ONLY. The v66.x SetCollision fallback only covered
       GenericColObj/KWBlockingVolume and never fired the Event, so the
       stock Mover wall stayed solid no matter what the mod poked.

- FIX (v67), per gap:
    1+2. The mod now replicates TurnOn WITHOUT the stock gate (a script
      TurnOn call would early-out: by sync time the gargoyle already lit
      the LEAD's light stock-side, and the gate checks PlayerHarry.bLumosOn
      only). Per driven player with a cold own-light:
        bLumosOn = True (bit write, hgame.LumosLight.bLumosOn)
        + the exact TurnDynamicLightOn register (LightEffect=13 and
          LightRadiusInner=5 ADDED to the v66.5 set; brightness written
          byte 255 or float 400.0 according to the layout tell
          brightnessIsFloatByLayout: LightHue - LightBrightness delta 1 =>
          byte property, >= 4 => float - the v66.5 byte-only write AND the
          byte-only detection were both silently wrong on a float engine,
          reading 400.0f as 0x00 = "off")
        + nativeSetLocation the light next to its owner (a cold light can
          still sit at the level-entry wand position)
        + spawn the stock HPParticle.LumosLightFX glow owned by the light
          (the visible glow; respawned if it dies, destroyed on off)
        + bLumosOn=True written on every driven pawn (the literal character
          state; stock keeps it on PlayerHarry only)
        + auto-off after the stock 30s (fLumosTimeToTurnOff) with the
          exact TurnDynamicLightOff register + particles destroyed - this
          breaks the "mod keeps light burning -> ratchet keeps state ->
          state keeps light burning" deadlock the light-follow timer would
          otherwise form with its own replication.
      The light's LOCATION is otherwise left to the STOCK wand Tick
      (UpdateLocation(WandEndPoint) while bLumosOn) - the mod only
      self-heals if the light drifts >300 units from its owner (property
      ever mismatched). The per-wand TheLumosLight offset is now resolved
      from the wand's OWN class token, walking the class chain for the
      declaring ancestor (hgame.<WandClassToken>.TheLumosLight) - the
      fixed "hgame.baseWand"/"hgame.HarryWand" attempts both missed on
      hardware. The owner-scan fallback now also accepts chain-proven
      LumosLight subclasses.
    3. The trigger pass in lumosTick: while the Lumos state is on, for each
      cached LumosTrigger/LumosSparklesTrigger with a non-empty Event that
      nobody has fired yet (stock's bFirstEventSent read from the actor,
      else the mod's once-per-level latch), if ANY tracked player stands
      inside the trigger's OWN radius (fDistanceCheck/fZDistanceCheck read
      from the actor, stock defaults 512/64 - stock only ever measured
      PlayerHarry), the mod fires the stock dispatch:
        manual Tag walk first: every live actor whose Tag == Event gets its
          own most-derived Trigger(Other=trigger, Instigator=near pawn)
          through ProcessEvent - chain-resolved, isActor-proven, never a
          foreign-class function (v66.4 lesson);
        Engine.Actor.TriggerEvent native (execThunk) as fallback.
      Once per level per trigger, exactly like stock's bFirstEventSent;
      the mod also SETS bFirstEventSent on triggers it fired so a later
      stock pass (P1 walking up) cannot toggle a TriggerToggle wall CLOSED.
      The v66.5 octree-safe SetCollision wall path stays as the fallback
      for collision-proxy walls.

- SAFETY NOTES:
    * Spawning a LumosLight for wands that lack one was considered and
      REJECTED: LumosLight.PreBeginPlay writes PlayerHarry.bLumosOn=False,
      so a mid-spell spawn would stomp the lead's live state. A wand
      without an own light logs once and gets no glow.
    * The Tag walk fires only chain-proven Actors (Engine.Actor in the
      receiver's class chain) and re-verifies trigger identity through
      lumosClassVerified before the Event is even read.
    * The dispatch order is deliberate: manual Tag walk (exact parms,
      proven ProcessEvent fire shape) before the engine native.

- VERIFICATION: host policy suites pass (tests/run.sh). The lumos suite
  adds section 12: the stock on/off light register values, both layout
  tells (brightness byte-vs-float, radiusInner adjacency), the type-aware
  isLightActiveF, the stock InLumosRadius predicate (512/64 defaults, Z
  gate), triggerShouldFire (the state machine minus the PlayerHarry key),
  companionLightShouldTurnOn and the 30s stock window. The Windows DLL
  could NOT be rebuilt in this sandbox (no i686 MinGW toolchain; package
  installs blocked) - build via CI (build-release.yml) and confirm line 2
  of hp3mod.log says build v67.

  HARDWARE CHECK LIST for the user:
    * Log line 2 says build v67 (else an old DLL is being tested).
    * The startup log gains a "[lumos] v67: ..." line: LightEffect /
      LightRadiusInner offsets, brightnessIsFloat (1 on a float engine),
      LumosLight.bLumosOn, Pawn.bLumosOn, Trig.bFirstEventSent,
      Trig.fDistanceCheck, execTriggerEvent, GlowFX class pointer. If
      bLumosOn=+0xFFFFFFFF the light replication still works (register
      only); if execTriggerEvent=0 the Tag walk covers the dispatch.
    * Cast Lumos as P2 at the gargoyle: expect
      "[lumos] v67 P1 TheLumosLight turned ON" is STOCK (its own TurnOn)
      and "[lumos] v67 P1 wand glow spawned" only for lights WE lit.
      P2/P3: "TheLumosLight turned ON", "wand glow spawned" - the wand
      now glows visibly.
    * Walk P2 into the revealed wall: expect
      "[lumos] v67 TriggerEvent fired: ... event=0x... instigator=..." -
      the wall OPENS (stock Mover path). If instead only the older
      "[lumos] v66.5 opened secret wall" line appears, the wall is a
      collision proxy, not a Mover - also fine.
    * The window ends 30s after the light: "auto-off after stock 30s".

  VERIFICATION (v67, completed):
    * tools/hostcheck/{windows.h,mmsystem.h,check.sh} - new repeatable
      host-side syntax check: a minimal hand-written Win32 stub surface
      for "g++ -fsyntax-only -std=c++11 -fms-extensions -Wall -Wextra".
      NOT part of the shipped DLL; the real build remains CI mingw32.
      First run found a REAL v67 bug: g_execTriggerEvent was declared
      void* but called as a function pointer (fixed to PFN_execActorFn
      with matching casts in resolveLumosFields). Also flushed out a
      write-only leftover sPrevBtn in the pad code (removed).
      Win32 stub notes for future edits: __declspec(x) must be #defined
      empty (host gcc has no __declspec), __stdcall empty, FARPROC
      void*, CreateThread's LPTHREAD_START_THREAD returns unsigned long.
    * tests/run.sh: ALL suites pass (lumos sync incl. v67 section 12a-f,
      stuck pawn, live index, frame carve, etc.).
    * dllmain.cpp structurally clean: braces/parens balanced, declaration
      order audited, -Wall -Wextra silent.

==========================================================================
26. V68 - WHY V67 STILL HAD NO P2/P3 GLOW AND NO WALL PASS: THE MENU-TIME
    RESOLUTION CACHE, THE UNATTACHED GLOW, THE NO-OP DISPATCH LATCH (2026-09-10)
==========================================================================

- FIELD REPORT (v67, same HP3_InsideHub gargoyle test as v66.5): the main
  character still gets the stock wand glow, but P2/P3 wands still never
  glow and P2/P3 still cannot walk through the revealed secret walls.

- THE THREE DEFECTS (all found by cross-checking the v66.5 hardware log
  against the v67 code, then verified against the ACTUAL HP2 decompile -
  metallicafan212/HP2UScriptDecompile master: LumosLight.uc, baseWand.uc,
  gargoyle.uc, LumosTrigger.uc, LumosSparkles.uc, HPParticle/LumosLightFX.uc):

  1. MENU-TIME ONE-SHOT FIELD RESOLUTION (the session-killer).
     resolveLumosFields was one-shot (static sResolved), and lumosTick runs
     every rendered frame INCLUDING THE MAIN MENU (the v66.1 perf finding
     documented that). The first resolution therefore happened before
     hgame/HPParticle/KWBlockingVolume were loaded. The v66.5 hardware log
     shows exactly this state: the family classes logged 3 of 4 NULL (only
     the menu-loaded KWGame.GenericColObj resolved) and
     TheLumosLight=+0xFFFFFFFF. Every hgame-side field was then cached
     NULL/-1 FOR THE WHOLE SESSION:
       g_clsLumosLightFX=NULL   -> the glow spawn block was dead code;
       g_offLumosBOn=-1         -> the wand Tick never rode the light;
       g_offTrig*=-1            -> trigger radii/bFirstEventSent unreachable;
       family class anchors NULL -> chain-proof admission dead.
     This alone explains both field symptoms: no replicated glow could ever
     spawn, and the stock wand Tick (which relocates light AND Particles via
     UpdateLocation while bLumosOn) never engaged for companions.
     FIX: engine-side fields (Engine.Actor.*, exports) stay one-shot; every
     hgame/HPParticle/KWGame-side field and class now resolves through ONE
     combined table pass (a single GetFullName walk with a per-object path
     match list, incl. fallback chains - baseWand/HarryWand.TheLumosLight,
     HPParticle/hgame LumosLightFX, harry/HPawn/HPCharacter.bLumosOn),
     retried at 1 Hz while anything is pending, with a hardware-log line
     "[lumos] v68 level-side fields resolved/partial: ..." naming every
     resolved offset/pointer.

  2. THE GLOW NEVER RODE THE WAND. The decompile shows stock TurnOn:
       Particles = Spawn(Class'LumosLightFX', self,, Location);
       Particles.EnableEmission(True);
     and baseWand.Tick: if (TheLumosLight.bLumosOn)
       TheLumosLight.UpdateLocation(WandEndPoint)  -> SetLocation(light)
     + Particles.SetLocation(NewLocation) - the glow rides the wand tip
     THROUGH the light's Particles pointer. v67 spawned the FX but never
     stored it in the light's Particles property (and never wrote bEmit),
     so the stock ride could never move the mod's glow and the emission
     state was left to chance.
     FIX: the replicated TurnOn now replays the stock sequence exactly:
     TurnDynamicLightOn register -> nativeSetLocation seed near the owner ->
     Spawn LumosLightFX owned by the light -> bEmit=True (property write,
     the StartGlowingWand-approved path) -> write the FX into the light's
     Particles -> ONLY THEN bLumosOn=True. bLumosOn is re-asserted every
     frame while the mod owns the light AND a live glow is attached (the
     bit is gated on the glow because the wand Tick dereferences Particles
     every frame while the bit reads True). TurnOff / 30s auto-off reverse
     the order: bLumosOn=False first, destroy the glow, null Particles. The
     300-unit self-heal stays as the safety net when the ride is unavailable.

  3. THE WALL DISPATCH COULD NO-OP AND LATCH. v67's lumosFireTriggerEvent
     ran the manual Tag walk FIRST. When a receiver's most-derived Trigger
     resolved to the empty Engine.Actor base event (the KWGame lineage
     walls - GenericColObj/KWBlockingVolume have empty Trigger overrides -
     and any state-declared Trigger the class-level lookup cannot see, e.g.
     a UE2 Mover's TriggerToggle.Trigger), the walk still counted it as a
     fire, LATCHED the once-per-level flag, logged "TriggerEvent fired ...
     wall opens" and suppressed the engine native forever. The wall stayed
     solid while the log claimed success.
     FIX: dispatch is now NATIVE execTriggerEvent first (the exact stock
     call, which resolves the operative Trigger including state-declared
     ones); the manual Tag walk is the fallback and now (a) skips receivers
     whose own chain declares no Trigger (no more counted no-ops), (b) finds
     state-declared Trigger functions (Pkg.Class.State.Trigger, most-derived
     declaring class wins), (c) logs the exact function path it runs. A
     failed fire no longer latches: it retries at 1 Hz
     (g_lumosTrigNextTryAt), so a linkage that appears late is not lost.

- TWO SECONDARY V68 HARDENINGS (both from the same audit):
  * The SetCollision wall fallback's player-near test measured from the
    WALL ACTOR's Location (300 uu). A large wall's Location can sit far
    past the surface a player presses against, so the open could never
    trigger even with the state engaged. The test now measures from the
    wall's PAIRED TRIGGER with the trigger's own fDistanceCheck /
    fZDistanceCheck (the stock InLumosRadius condition the level designer
    tuned), plus the wall-actor proximity as a secondary
    (hp3lumos::wallShouldOpenProxied, regression-tested; pair index stored
    at scan time in g_lumosWallTrig).
  * A level scan that found nothing (the menu case) was final - if no
    camera-package invalidation fired before the first level, the first
    level's walls were never cached. An empty result now re-scans quietly
    at 1 Hz until the level's actors appear.

- VERIFICATION: tests/run.sh ALL suites pass (lumos suite gains section 13:
  the wallShouldOpenProxied truth table, and section 14: the v68 retry
  cadence constants). tools/hostcheck/check.sh: dllmain.cpp syntax clean
  (-Wall -Wextra, stub Win32 surface). The Windows DLL could NOT be rebuilt
  in this sandbox (no i686 MinGW toolchain) - build via CI
  (build-release.yml) and confirm line 2 of hp3mod.log says build v68.

  HARDWARE CHECK LIST for the user:
    * Log line 2 says build v68.
    * "[lumos] v68 level-side fields resolved: Wand=+0x.. TheLumosLight=
      +0x.. LumosLight.Particles=+0x.. LumosLight.bLumosOn=+0x..(0x..)
      Trig.fDistanceCheck=+0x.. execTriggerEvent=........ GlowFX=........"
      prints NON-trivial values (v67's failure mode was all of these
      unresolved, cached from the menu). If it says PARTIAL, tell us which.
    * Cast Lumos as P2 at the gargoyle: expect
      "[lumos] v68 P1.. (stock)" nothing / "[lumos] v68 P%d TheLumosLight
      turned ON", then "[lumos] v68 P%d wand glow spawned + attached ..." -
      the wand tip now glows visibly and the glow FOLLOWS the wand.
    * Walk P2 into the revealed wall: expect either
      "[lumos] v68 TriggerEvent fired (native execTriggerEvent): ... event=
      0x.." or (no exec export) "[lumos] v68 Tag walk fires <real Trigger>
      on <wall> via own Trigger". If the log instead shows "no TriggerEvent
      dispatch yet ... retrying at 1 Hz", the wall has no Event linkage and
      the SetCollision fallback must do the work - expect
      "[lumos] v66.5 opened secret wall #k" with the trigger-proxied test.
    * The window ends 30 s after the light: "auto-off after stock 30s".

==========================================================================
27. V69 - ROUND 3: THE STOCK SCRIPT RUNS THE GLOW; EVENT==TAG WALL
    LINKAGE; THE EMPTY-WEAPON RIDE KILLER (2026-09-10)
==========================================================================

- FIELD REPORT (v68, same HP3_InsideHub gargoyle test as v66.5/v67): the
  lead still gets the native wand glow, P2/P3 wands still never glow and
  P2/P3 still cannot pass the revealed secret walls.

- METHOD: this round was checked against the ACTUAL HP2 decompile
  (github.com/metallicafan212/HP2UScriptDecompile master, cloned and read
  verbatim: HGame/Classes/Misc/LumosLight.uc, HGame/Classes/baseWand.uc,
  HGame/Classes/Misc/gargoyle.uc, HGame/Classes/Triggers/LumosTrigger.uc,
  HGame/Classes/Triggers/LumosSparkles.uc, HPParticle/Classes/
  LumosLightFX.uc). Four defects fell out.

- DEFECT 1 - THE GLOW WAS A CHAIN OF RUNTIME GUESSES (why three builds in
  a row could all "look right" and still ship dark wands). Stock TurnOn is
  ONE scripted call that does everything:
      bLumosOn=True; PlayerHarry.bLumosOn=True; Enable('Tick');
      TurnDynamicLightOn(); foreach AllActors A.OnLumosOn();
      Particles = Spawn(Class'LumosLightFX', self,, Location);
      Particles.EnableEmission(True);
  v66..v68 replayed it by hand and needed EVERY piece to resolve at
  runtime: g_clsLumosLightFX (HPParticle.LumosLightFX by exact path), the
  LumosLight.Particles offset, the ParticleFX.bEmit offset+bitmask, the
  execSpawn bytecode synthesis - and bLumosOn was gated on the spawned FX
  being live. ANY single miss = dark wand, dead ride, no log contradiction.
  FIX (the strategy change): for each companion with a cold own light the
  mod now calls the light's OWN stock script - ProcessEvent(light,
  Function hgame.LumosLight.TurnOn) - the exact bytecode that glows P1's
  wand natively. Zero dependency on the mod resolving any glow piece.
  The one stock behaviour the call cannot pass by itself is the
  single-player gate `if (PlayerHarry.bLumosOn) return;` - PlayerHarry is
  the LIGHT's own var (LumosLight.PreBeginPlay: PlayerHarry = harry(Level.
  PlayerHarryActor)), i.e. the LEAD, already burning by sync time. The
  gate dance: mask every driven pawn's bLumosOn bit (read, clear), make
  the ONE synchronous ProcessEvent call (no other script can run mid-call
  on the game thread), restore the saved bits; TurnOn re-asserts
  PlayerHarry.bLumosOn=True itself. The mod then RE-READS the light: if it
  still reads cold (unreadable gate var -> PlayerHarry None -> script
  abort, or fn unresolved) the v68 register+spawn replication runs as the
  automatic fallback. TurnOff gets the same treatment on state expiry
  (stock teardown INCLUDING the OnLumosOff broadcast the register replay
  never sent). A stock-lit light observed cold is the stock 30 s auto-off:
  latched one-shot, never re-timed or re-lit by the mod.

- DEFECT 2 - THE EMPTY-WEAPON RIDE KILLER (a stock-code fact that made
  every previous glow spawn pointless the moment the ride ran).
  baseWand.Tick:
      local Vector WandEndPoint;      <- starts (0,0,0)
      if ( (Pawn(Owner) != None) && (Pawn(Owner).Weapon == self) ) {
          ... WandEndPoint = GetWandEndPoint(); ...
      }
      if ( TheLumosLight.bLumosOn )
          TheLumosLight.UpdateLocation(WandEndPoint);   <- SetLocation(light)
                                                         Particles.SetLocation
  WandEndPoint is assigned ONLY inside the Weapon==self branch. A
  companion whose Engine.Pawn.Weapon slot is EMPTY therefore had the stock
  ride write the UNASSIGNED LOCAL (0,0,0) over the light and its Particles
  EVERY FRAME - light and glow teleported to the world origin, wand dark,
  no matter how correctly the mod had spawned and attached the glow.
  (And UpdateLocation has no None-check on Particles, so a None Particles
  aborts UpdateLocation after the light's SetLocation - the light fights
  the (0,0,0) ride alone while the glow is gone entirely.) FIX: while the
  replication is active, an EMPTY Weapon slot is filled ONCE with the
  companion's own wand actor (a read-back check; an existing Weapon is
  never overwritten) so the stock ride target exists - then the stock
  wand Tick moves light+glow onto the companion's wand tip exactly like
  stock does for the lead. The 300-unit self-heal stays as the no-ride
  safety net and now carries the tracked glow with the light.

- DEFECT 3 - SECRET-WALL PAIRING IGNORED THE STOCK LINKAGE. LumosTrigger
  opens its wall with TriggerEvent(Event, self, None); the engine
  dispatches that Event to every actor whose Tag EQUALS the Event name -
  raw 4-byte name-index equality, zero distance involved. The v66.5
  pairing measured ONLY distance (SecretWallPairRadius = 900 from the
  trigger): a large wall whose actor centre sits >900 units from its
  trigger was classified ORDINARY, so the octree-safe SetCollision
  fallback never even considered it - a silent lockout with no log line.
  FIX: pairing now FIRST tests wall.Tag == trigger.Event (two guarded
  dword reads, hp3lumos::wallEventLinksToTrigger, regression-tested); the
  900-unit radius pairing stays as the secondary heuristic. The scan log
  line splits the counts ("v69 N by Event==Tag stock linkage, M by
  proximity").

- DEFECT 4 - THE EXACT-TOKEN TRIGGER CACHE COULD BE EMPTY BY NAME ALONE.
  The decompile's real placed sparkles class is "LumosSparkles"
  (Triggers/LumosSparkles.uc) - NOT "LumosSparklesTrigger"; the v66.4
  exact-name rule rejected the real class, and any HP3-only subclass name
  (e.g. "LumosTriggerLarge") too. A level whose only Lumos triggers carry
  such names cached ZERO triggers: no pairing, no fire, no secret walls -
  again silently. FIX: "LumosSparkles" joins the exact-token list
  (ground truth from the decompile), and Lumos-named look-alikes can now
  prove themselves against the Engine.Triggers family anchor
  (LumosTrigger and LumosSparkles both declare `extends Triggers`) via
  the chain POINTER proof - a name can never fake a pointer, and the
  crash-history impostors still reject (LumosSparklesEmitter is an
  Engine.Emitter subclass, textures/UClasses have no Triggers in their
  chain, and "LumosLight" never even matches the Lumos-trigger look-alike
  token filter). The anchor resolves in the DEFERRED table (a one-shot
  engine-block lookup would repeat the v68 menu-time miss).

- VERIFICATION: tests/run.sh ALL suites pass. The lumos suite gains
  section 15 (wallEventLinksToTrigger: linked, mismatched, zero-Tag,
  zero-Event, both-zero) and section 16 (LumosSparkles exact token
  admitted; LumosSparklesEmitter / LumosTriggerLarge / LumosLight /
  plain Trigger / empty all rejected; the verbatim v66.3/v66.4 crash
  receivers still reject in isLumosTriggerObject), and 10b is updated to
  the decompile-correct rule. tools/hostcheck/check.sh: dllmain.cpp
  syntax clean (-Wall -Wextra, stub Win32 surface). The Windows DLL could
  NOT be rebuilt in this sandbox (no i686 MinGW toolchain) - build via CI
  (build-release.yml) and confirm line 2 of hp3mod.log says build v69.

  HARDWARE CHECK LIST for the user:
    * Log line 2 says build v69.
    * "[lumos] v69 level-side fields resolved: ... TurnOn=........
      TurnOff=........" - TurnOn/TurnOff are the stock script function
      pointers; if they are 0 the v68 fallback carries the glow instead
      (the log says which path ran).
    * The scan line now reads "... v69 N by Event==Tag stock linkage,
      M by proximity ..." - N+M must be >= 1 in a level with a Lumos
      secret wall. N=0 AND M=0 means the level's walls never got paired:
      report that line.
    * Cast Lumos as P2 at the gargoyle: expect
      "    -> ProcessEvent LumosLight.TurnOn [stock replication]" then
      "[lumos] v69 P1.. nothing (stock) / P%d TheLumosLight turned ON via
      the STOCK TurnOn script ... glow=0x..." - P2/P3 wand tips glow and
      the glow FOLLOWS each companion's wand (stock ride). If instead the
      log shows "stock TurnOn did not engage" or "v68 fallback register",
      the glow is on the fallback path - still glows, but report it.
    * Walk P2 into the revealed wall: expect either "[lumos] v68
      TriggerEvent fired (native execTriggerEvent)" / "Tag walk fires
      <real Trigger>" (stock Mover opens - permanent, like stock), or
      "[lumos] v66.5 opened secret wall #k" (SetCollision proxy opens
      while the window burns). P2/P3 pass through.
    * After 30 s: "[lumos] v69 P%d light went cold on its own (stock Tick
      auto-off ...)" for stock-lit companions - the window ends stock-side.

v70 (2026-09-10) - LUMOS P2/P3 ROUND 4: THE v69 FIELD LOG DECODED
------------------------------------------------------------------
Same HP3_InsideHub gargoyle test, 2 players, P2 = Hermione casts Lumos at
gargoyle6. Lead glows natively; P2's wand dark; P2 cannot pass the wall.
Every symptom is explained by lines already in that log:

- "[lumos] v69 level-side fields PARTIAL: ... Pawn.bLumosOn=+0xFFFFFFFE(0x0)"
  The pawn's bLumosOn bit NEVER resolved. The v69 table guessed
  hgame.harry / hgame.HPawn / hgame.HPCharacter .bLumosOn; HP3 renamed the
  hierarchy (the same log shows hgame.HPPawn, hgame.HPHeroPawn and the wand
  class hgame.WeaponWand - none of the HP2 names). Consequence: the v69
  "gate dance" masked NOTHING. PlayerHarry.bLumosOn stayed True (the
  gargoyle had just lit the lead), stock LumosLight.TurnOn hit
  `if (PlayerHarry.bLumosOn) return;` and did nothing:
     "-> ProcessEvent LumosLight.TurnOn [stock replication]"
     "[lumos] v69 P1 stock TurnOn did not engage (light still cold)"
  FIX: resolve bLumosOn by walking the LIVE pawn's own class chain
  (lumosResolvePawnLumosBit - the exact walk that found TheLumosLight on
  hgame.WeaponWand in v68), and add a PRIMARY gate that needs no pawn bit
  at all: for the one synchronous TurnOn call the light's OWN PlayerHarry
  var (LumosLight.PlayerHarry, chain-resolved from the live light) is
  retargeted at the companion. Stock then reads the gate off the companion
  (whose bit the mod holds FALSE for the call), writes
  PlayerHarry.bLumosOn=True ONTO THE COMPANION and spawns the glow - the
  literal stock semantics, per player. The var is restored right after the
  call (Tick/TurnOff/ClientMessage keep talking to the lead). The retarget
  is only done when the companion's class declares bLumosOn at the resolved
  offset (lumosPawnHasLumosBit, cached per class) - stock declares it on
  harry, and writing harry's offset inside a Hermione object would be a
  foreign-field poke; otherwise the chain-resolved bit mask on the lead
  (the v69 dance, now with a real offset) is the gate. The lead's own bit is
  never written by the mod any more (the stock chain owns it).

- "[lumos] v69 P1 TheLumosLight turned ON (v68 fallback register)" then,
  16 ms later, "[lumos] v69 P1 light went cold on its own (stock Tick
  auto-off ...) - one shot per cast". NOT a stock auto-off. The v68
  fallback deliberately holds LumosLight.bLumosOn LOW until a glow exists
  (UpdateLocation dereferences Particles), and GlowFX=00000000 in the same
  log (HPParticle.LumosLightFX does not exist on HP3 by that path) meant no
  glow ever existed - so lumosLightBurning() (which reads bLumosOn, resolved
  +0x3F8) returned cold on the very next frame and the observation latch
  declared the cast dead. The light was ON (register written) for one
  frame. FIX: the auto-off observation applies to STOCK-lit lights only
  (hp3lumos::observedStockAutoOff, regression-tested); the fallback keeps
  the mod's own 30 s timer as before. LumosLightFX is now also resolved by
  CLASS TOKEN in any package (LRK_CLASSTOKEN "*.LumosLightFX" - HP3 moved
  it out of HPParticle), and a glow-less fallback light is ridden onto the
  companion every frame by the mod (the stock ride is off while the bit is
  low), so at least the dynamic light travels with P2.

- "[lumos] v68 Tag walk fires Function Engine.Actor.Trigger on 10e0c800 via
  Engine.Actor.Trigger (EMPTY base - no-op!)" and the pelog line
  "LumosSparklesTrigger HP3_InsideHub.LumosSparklesTrigger5 ::
  Function Engine.Actor.Trigger other=LumosTrigger HP3_InsideHub.LumosTrigger0"
  Two things. (a) lumosTriggerFnFor's class-level loop walked the chain
  down to Engine.Actor, found the EMPTY base Trigger there and returned it
  - the state-declared pass (Pkg.Class.State.Trigger) that v68 added for
  Movers NEVER RAN. (b) The receiver of LumosTrigger0's Event is the
  LumosSparklesTrigger - HP3 wires LumosTrigger --Event--> LumosSparkles
  Trigger --Event--> wall (the scan's 4 Event==Tag pairings are
  sparkles.Event == wall.Tag). The walk fired the empty base on the
  sparkles, counted that as a fire, latched the slot, and the wall's real
  linkage one hop further was never dispatched. Meanwhile the pelog shows
  Hermione Bump-ing something at the wall and touching GenericColObj2 -
  the mod had opened "#63", but the log never said which actor #63 was
  nor what she bumped. FIX: (1) fire the SCRIPT Engine.Actor.TriggerEvent
  (UE2: a plain UnrealScript function; the HP2 DuelTrigger overrides its
  exact signature) through ProcessEvent ON THE TRIGGER - byte for byte
  what stock StateLumosOn.BeginState does - so the ENGINE performs the
  virtual Trigger dispatch on every Tag-matched receiver, state-declared
  Mover Triggers included; (2) relay an EMPTY-Trigger receiver's own Event
  one hop onward the same way (lumosRelayEventChain, depth <= 3, every
  receiver named with its Tag/Event) - that is how the sparkles hop
  reaches the wall; (3) the manual Tag walk (fallback) never counts the
  empty base as a fire and relays through it instead; (4) the chain
  lookup stops before Engine.Actor so the state pass actually runs.

- Diagnostics: "[lumos] v69 level-side fields PARTIAL" was logged EVERY
  FRAME for a second (condition evaluated per frame, sAttempts advanced
  per second) - once per attempt now. The scan lists every cached trigger
  (name, Tag, Event, location) and every secret wall (name, Tag, paired
  trigger, pristine bits, location); an opened wall is logged by NAME and
  position; the pelog prints the Bump partner ("other=...") so the next
  log names whatever still blocks a companion at a revealed wall.

  tools/hostcheck/check.sh: syntax clean (-Wall -Wextra). tests/run.sh:
  all suites pass (lumos: +observedStockAutoOff). The Windows DLL could NOT
  be rebuilt in this sandbox (no i686 MinGW toolchain, no root) - build via
  CI (build-release.yml) and confirm line 2 of hp3mod.log says build v70.

  HARDWARE CHECK LIST:
    * Log line 2 says build v70.
    * "[lumos] v70 Pawn.bLumosOn resolved on hgame.<class> = +0x... mask
      0x..." and "[lumos] v70 LumosLight.PlayerHarry resolved (+0x...)".
      If either says NOT FOUND, report the line.
    * "[lumos] v70 class token *.LumosLightFX resolved to Class <pkg>.
      LumosLightFX" - tells us where HP3 keeps the glow class.
    * P2 casts Lumos at the gargoyle: expect "[lumos] v69 P1 TheLumosLight
      turned ON via the STOCK TurnOn script ... glow=0x<nonzero>" and NO
      "did not engage". P2's wand tip glows and the glow follows the wand.
    * Walk P2 to the wall: expect "[lumos] v68 TriggerEvent fired (script
      Engine.Actor.TriggerEvent ...)" followed by "[lumos] v70 event
      receiver[1]: LumosSparklesTrigger... (own Trigger: EMPTY base,
      Event=0x....)" and "relaying ... via script TriggerEvent" and then
      the wall receiver line. If P2 still bumps, the pelog now says
      "HPCharacter.Bump other=<actor>" - report that actor's line and the
      "secret wall #k <name> ..." lines from the scan.
